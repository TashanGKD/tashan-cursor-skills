---
description: 经验自动感知钩子。任务结束时执行三步收尾：①回顾过程感知A-G信号并写入PENDING-EXPERIENCES（G=结构性根因：失败根因是规范/机制本身设计缺陷，而非执行疏忽）；②自动写入任务日志.md（不输出提醒，直接写文件）；③告知用户。禁止输出"请记得..."类的提醒文字。任何实质性任务完成后必须触发（由 role-menu 强制规则第14条约束执行）。
alwaysApply: false
---

> ⚠️ **[2026-03-23] 本规则已被 session-bootstrap.mdc 的 B1/B2/B4 步骤实质性覆盖**
>
> **状态**：功能性冗余（Deprecated）。本规则与 session-bootstrap.mdc 序列B 功能完全重叠：
> - 本规则的 Step 1 = session-bootstrap B1（经验感知 → PENDING-EXPERIENCES）
> - 本规则的 Step 1.5 = session-bootstrap B2（积压感知）
> - 本规则的 Step 2/3 = session-bootstrap B3/B4（告知用户 + 写任务日志）
>
> **根因**：session-bootstrap.mdc（alwaysApply:true，步骤序列形式，执行可靠性高）在
> 2026-03-21 创建后实质替代了本规则。本规则是历史遗产，保留为参考文档，不再是执行入口。
>
> **影响**：本规则 alwaysApply: false，实际已通过 role-menu Rule 14 → session-bootstrap 执行。
> 如需修改经验感知行为，**修改 session-bootstrap.mdc B1/B2/B4**，不再修改本文件。

> ⚠️ **本规则不再 alwaysApply（P0-3 修复，2026-03-20）**
> 目的：减少每次对话的 token 注入（原 ~3000 token/次），行为不变。
> role-menu.mdc 强制规则第14条保证：任何实质性任务完成后，AI 必须执行本规则的三步收尾。

> ⚠️ **同步维护说明**：本文件的 Step 1/1.5/2/3 在 `.cursor/rules/role-menu.mdc` 强制规则第14条中也有内联副本（因 hook 是 alwaysApply:false，role-menu 是 alwaysApply:true，两处需保持一致）。修改本文件任意 Step 时，**必须同步修改 role-menu.mdc Rule 14 对应描述**。

# 经验自动感知钩子（auto-experience-hook）

> 核心原则：**执行时专注执行，结束时回顾沉淀**。
> 不要在任务中途监控信号——结束后回顾比中途打断更可靠。

---

## 触发信号定义（A-E 类）

以下信号类型用于「任务结束回顾」步骤中的判断标准：

```
信号 A：踩坑信号
  → 遇到了报错/失败，且找到了解决方案（不是放弃）

信号 B：新发现信号
 → 完成了某个步骤，但这个步骤在现有 Skill 流程中没有覆盖
 → 判断标准：如果下次不记得，同类任务会重蹈覆辙
 → 特别触发：若本次对话发现 `_内部总控/知识库/` 目录下有文件，视为信号B
   （该目录已废弃，正确位置：`认知结构/L1_系统性文档/[维度]/知识库/`；参见 `_内部总控/知识库放置规范.md`）

信号 C：步骤偏差信号
  → 实际执行与 Skill 描述的步骤顺序/内容不一致

信号 D：触发词不准信号
  → 某个 Skill 本该触发却没触发，或触发了不该触发的 Skill

信号 E：缺失 Skill 信号
  → 遇到某类任务，没有对应 Skill，AI 凭经验执行
  → 且这类任务未来可能重复出现

信号 G：结构性根因信号（元反思层）
  → AI 发现本次失败/偏差的根因不是执行疏忽，而是规范/机制本身设计有缺陷
  → 判断标准（满足任一即触发）：
      · 某行为今天又出错了，但类似情况之前已经出现过（≥2次同类失败）
      · 发现某条规范只在某个 Skill 里，但这条规范应该普遍生效（应是 alwaysApply Rule）
      · 发现某个重要文档/规范，AI 没有通过 D0/Rule 获知，靠「记忆」才知道要读
      · 发现规范写的是「建议/应该」，但实际需要「禁止/必须」级别的强制
      · 发现某条规范的覆盖范围写错了（太窄/太宽），或表述有歧义
  → 记录格式（追加到 PENDING-EXPERIENCES，类型写「结构性根因」）：
      | [今日日期] | [触发信号的 Skill 名] | 结构性根因 | [缺陷类型: 连接缺口/类型错误/执行力度/覆盖错误/表述歧义]——[一句话描述：如「role-Skill产品经理没有D0行，不读原则文档，是连接缺口而非执行疏忽」] | 🔲 待处理 |
  → ⚠️ 注意：触发信号G时，PENDING-EXPERIENCES 的记录格式要含「缺陷类型」字段，方便 project-retrospective 识别为 E6 类条目

信号 F：用户认知输入信号
  → 用户在对话中表达了具有学习/演进价值的思考、原则或洞见
  → 判断标准（满足任一即触发）：
      · 用户使用「应该...」「肯定应该...」「这是个原则...」「这是个底层问题...」
      · 用户使用「这里有个问题...」「我发现...」「根本原因是...」
      · 用户对系统行为/设计模式做出规律性总结（如「所有X都应该Y」「AI总是Z」）
  → 判断排除：用户只是提问（「这是什么？」）或只是要求执行任务（「帮我写X」），不触发
  → 记录格式（追加到 PENDING-EXPERIENCES，类型写「认知碎片」）：
      | [今日日期] | [当前对话主题/任务名] | 认知碎片 | [用户核心洞见一句话] | 🔲 待处理 |
  → 后续处理：project-retrospective 遇到「认知碎片」类条目时，调用 cognitive-capture-fragment 写入 L2，候选为 L1.5 原则
  → 注意：本次对话若已直接写入了 L2 碎片，PENDING-EXPERIENCES 可标注「已写入L2，见F-XXX」
```

信号 G：工具使用违规信号
  → AI 使用了规范明确禁止的工具/手段完成文件操作或其他任务
  → 判断标准（满足任一即触发）：
      · 用 Shell/Python/PowerShell 写入或修改文件（等价于 echo/cat 写文件，违反工具使用规范）
      · 用 grep/find/cat/head/tail 等 shell 命令做搜索/读取（应用 Grep/Glob/Read 工具）
      · 任何「技术上可行但规范明确禁止」的操作
  → 特别说明：「Python 脚本写文件」和「echo > file」在意图上等价，均属违规
  → 触发时机：任务结束回顾时，如果发现本次有以上操作，必须触发

  **合规例外场景（满足以下全部条件时，Python 写文件不触发 G 信号）**：
  条件一：文件内容超过 Write 工具的合理处理范围（经验阈值：>2MB 或需要外部库处理如 base64/markdown）
  条件二：必须在执行前向用户声明「本任务需要用 Python 脚本生成大文件，因内容超出 Write 工具合理范围」
  条件三：Python 脚本以 Write 工具写入工作区（不用 Shell 直接写），或使用 Shell 执行已存在的脚本文件
  条件四：不允许静默使用——若未提前声明，即使文件很大也属违规
  典型合规场景：35MB HTML 含 24 张 base64 图片的生成；含 qwen-image API 调用和 base64 嵌入的图片生成脚本

> ⚠️ 技术困难时的强制规范（防止「聪明绕过」）：
> 遇到技术困难（如编码问题、API不可用）时，禁止静默选择违规替代方案。
> 强制流程：
> 1. 明确说明遇到了什么困难
> 2. 检查替代方案是否违反规范（含上方「合规例外场景」检查）
> 3. 如果替代方案违规，向用户说明并请求决策
> 绝不允许：静默使用违规方案 + 事后辩解「这是合理的技术变通」

**不记录的情况**（避免暂存区被垃圾填满）：
- 顺利执行了已有 Skill 的标准流程，无任何偏差
- 遇到的问题在踩坑速查中已有记录，直接查到解决方案
- 一次性特殊需求，不会在其他任务复现

---

## 任务结束时执行（所有实质性任务完成后）

**触发条件**：任何实质性任务处理完毕时。
包括：角色类 Skill 执行完成、用户的具体指令已处理完、用户说「好」「完成」「可以」等确认词。

```
Step 1  【经验回顾】（耗时 < 10 秒，不阻断对话）
        回顾刚才的执行过程，判断是否出现了 A-E 任意一类信号：
        
        IF 发现信号：
          直接追加一行到 .cursor/skills/skill-index/PENDING-EXPERIENCES.md：
          | [今日日期] | [来源Skill或任务描述] | [信号类型] | [一句话描述经验内容] | 🔲 待处理 |
          （不需要调用子智能体，直接写文件，耗时 < 5 秒）
        
        IF 没有发现信号：
          跳过，不写入，不提示

Step 1.5 【Algedonic 积压检查】（F-049，Beer 1972 §16.7 时间约束机制）
         读取 PENDING-EXPERIENCES.md 中所有「🔲 待处理」条目的日期列：
         
         IF 最老条目 > 30 天：
           在收尾输出末尾追加（明显标注）：
           「⚠️ 积压警告：最老待处理经验已 M 天，建议本次触发 project-retrospective」
         
         ELSE IF 最老条目 > 7 天：
           在收尾输出末尾追加（低调提示）：
           「💡 积压提示：有 N 条待处理经验，最老已 M 天，建议下次任务后处理」
         
         ELSE（所有条目 ≤ 7 天，或无待处理条目）：
           跳过，不输出任何提示

Step 2  【任务完成收尾】
        若有经验被记录：
          告知用户：「✅ 任务完成。本次记录了 N 条经验（已暂存），
                     确认跑通后可说「做一次项目复盘」处理。」
        
        若无经验被记录：
          简短告知：「✅ 任务完成。」

Step 3  【自动写入任务日志】（RULE-01 执行层落地）

        ⚠️ 执行顺序铁律（不可颠倒）：
        工具调用 → 成功确认 → 告知用户
        禁止：先输出「📝 已写入」文字，再执行工具调用（或不执行）

        所有任务完成后，AI 按以下顺序执行：

        1. [工具调用-读取] Read: t:\TashanAgent4S_2026\0310_huaxiang\_内部总控\任务日志.md
           → 找到今日（YYYYMMDD）已有的最大 NN 序号，新条目序号 = NN+1（当日第一条为 01）
           → ⚠️ 使用 grep 找今日全部 `## [TASK-YYYYMMDD-` 条目取最大值（不只看末尾），防止并行 session 已在末尾写入更大序号

        【多 session 并发冲突规避】
        当工作区同时有多个 AI session 并行工作时，任务日志序号可能产生冲突（不同 session 各自读到相同序号）：
        - 规避方案：若写入后发现序号已被占用（文件中已有相同 TASK-ID），改用更大序号或追加随机2位字母后缀（如 TASK-20260321-33a、33b），不得覆盖已有记录
        - 检查方式：写入前再次确认文件中不存在相同 TASK-ID；若存在，序号 +1 后重试
        - 注意：这是并发写入的固有竞争条件，无法完全避免，只能通过「全文搜索取最大值 + 后缀」策略降低冲突概率

        2. [工具调用-写入] StrReplace 追加到文件末尾（此步骤必须产生实际的工具调用，不是文字描述）：
           ---

           ## [TASK-YYYYMMDD-NN] <任务名称>

           **日期**：YYYY-MM-DD
           **执行者**：AI（Claude）
           **触发来源**：用户需求

           ### 完成的工作
           <本次任务的核心操作与关键决策>

           ### 创建/修改的文件
           <逐条列出>

           ### 用户原始需求
           <用户的原始说法>

           ### 遗留事项
           <未完成项，无则写"无">

        3. [确认后告知] 工具调用成功返回（无报错）后，才输出：
           「📝 任务日志已自动写入 [TASK-YYYYMMDD-NN]」

           若工具调用报错 → 输出「⚠️ 任务日志写入失败，请手动检查 _内部总控/任务日志.md」
           若遗漏了工具调用 → 用户询问时立即补做，不可以「已声称完成」替代实际执行
```

> ⚠️ **禁止声明虚假完成**：
> AI 绝对不得输出「任务日志已自动写入」「已追加到任务日志」等声称已完成写入的文字，
> 除非实际执行了文件写入工具调用（Write/StrReplace/Shell）。
> 如果只是提醒用户去写，必须使用「请记得追加...」「建议你写入...」等主动语态，
> 不得使用被动完成态（「已写入」「已追加」）。

**写入格式示例**：

```
| 2026-03-19 | role-后端开发 | 踩坑 | FastAPI response_model 返回 None 时抛 422，需用 Optional 包裹 | 🔲 待处理 |
| 2026-03-19 | 直接任务-HTML完善 | 新发现 | 微信文章 HTML 中 img 标签需要 data-src 而非 src 才能正常显示 | 🔲 待处理 |
| 2026-03-19 | role-测试工程师 | 步骤偏差 | 关卡C 的接口测试应在 DB 初始化之后执行，当前 Skill 描述顺序相反 | 🔲 待处理 |
```

注意：「来源」字段可以是 Skill 名称，也可以是任务描述（如「直接任务-HTML完善」）。

---

## 与其他组件的关系

| 组件 | 关系 |
|---|---|
| `project-retrospective` | 消费暂存文件，批量处理所有 🔲 待处理 条目 |
| `skill-capture-closure` | 被 project-retrospective 调用，处理每条具体经验 |
| `skill-rule-修改规范` | 本规则修改时须遵循其流程 |
| `experience-logger.md`（Agent）| 已弃用——本版恢复直接写文件，不再调用该 Agent |

---

## 变更记录

### 2026-03-19 — 初始创建

任务完成收尾段：仅在有待处理条目时提示用户。

### 2026-03-19 — 加入 RULE-01 执行层落地（任务日志提醒）

任务完成收尾时，无论是否有经验暂存，均追加任务日志提醒。

### 2026-03-19 — 改为调用 experience-logger 子智能体（已证明是退步）

将直接写文件改为调用后台子智能体。实践中不可靠（AI单线程，无法真正后台异步）。

### 2026-03-19 — v2.0 回归直接写文件 + 三处结构性修复

**根因**：对比20260319c版本发现三处退步：①子智能体调用不可靠；②收尾触发条件仅限「角色类Skill完成后」遗漏直接任务；③信号检测要求任务中途监控，单线程下认知竞争。

**修改内容**：
- 修改：暂存操作 → 恢复直接写文件（去掉 experience-logger 调用，追加一行即可）
- 修改：收尾触发条件 → 从「角色类Skill最终步骤完成后」改为「任何实质性任务完成后」
- 修改：信号检测时机 → 从「任务中途监控」改为「任务结束时回顾」（三步合并为一段：回顾→收尾→日志）

**验证结果**：
- 正向验证：在非Skill框架的直接任务结束时，AI回顾过程，有踩坑时写入暂存区
- 负向验证：顺利的标准任务不产生无意义条目

**验证状态**：🔵 待验证（下次任务执行时验证）

---

### 2026-03-19 — v2.0 → v2.1 — Step 3 从「提醒」改为「自动写入」

**根因**：本次任务完成时，Step 3 输出了"请记得在任务日志.md追加..."的提醒文字，用户明确指出这应该是 AI 自动执行的动作而非提醒。这是 Step 3 设计时的明确错误：它把应该是「动作」的事情设计成了「提醒」。

**修改内容**：
- 修改：Step 3 标题 → 从「任务日志提醒」改为「自动写入任务日志」
- 修改：Step 3 内容 → 从「输出提醒文字让用户手动操作」改为「AI 直接读取日志文件、确定序号、追加条目、告知写入结果」

**验证结果**：
- 正向验证：下次任务完成时，AI 应直接写入 任务日志.md，不输出"请记得"类文字（待真实场景验证）
- 负向验证：Step 1（经验回顾）和 Step 2（收尾告知）行为不变

**验证状态**：🔵 待验证（本次任务完成后立即验证）

### 2026-03-19 — v2.1 → v2.2 — 修复 description 字段误导性描述

**根因**：description 字段写的是「并输出任务日志提醒」，与 Step 3 的「自动写入」设计矛盾。AI 读到 description 即确立行为模式，description 层的「提醒」覆盖了 Step 3 的「写文件」。这是为什么即使 Step 3 已改为自动写入，实践中仍然输出「请记得」文字的根本原因。

**修改内容**：
- 修改：description 字段 → 明确「自动写入任务日志.md」「禁止输出'请记得...'类提醒文字」

**验证状态**：🔵 待验证

### 2026-03-19 — 禁止声明虚假完成（步骤偏差修复）

**根因**：AI 输出「任务日志已自动写入」但实际未执行写入，用户通过 Grep 发现缺口。声明完成态但未实际执行工具调用，属于 R2 NO_FABRICATION 的边界违反。

**修改内容**：追加「禁止声明虚假完成」约束到任务完成收尾段（Step 3 代码块之后），明确规定：只有工具调用成功返回后才能使用被动完成态，否则必须使用主动提醒语态。

**验证状态**：🔵 待验证

---

### 2026-03-19 — v2.3 → v2.4 — 信号B 增加 REF-EXT 错误位置检测

**根因**：另一对话在 `_内部总控/知识库/`（已废弃的旧位置）创建了3个 REF-EXT 文件（OpenCode三方对比/软件保护调研/开源AI框架全景），说明 REF-EXT 规范（存于 `维护协议_自洽规范.md`）对其他对话不可见，导致反复在旧位置创建文件。

**修改内容**：
- 修改：信号B 判断标准 → 新增「特别触发」条件：发现 `_内部总控/知识库/` 有文件时视为信号B，注明正确位置

**验证结果**：
- 正向验证：下次发现该目录有文件时，Step 1 回顾应识别为信号B（待真实场景验证）
- 负向验证：无文件在旧位置时不产生额外噪音，其他信号类型不受影响

**验证状态**：🔵 待验证

### 2026-03-19 — v2.2 → v2.3 — Step 3 加入「工具调用优先」铁律（结构性修复）

**根因**：「声称完成≠实际执行」问题第N次复现（TASK-07/10/12等均宣称写入但实际未执行工具调用）。v2.0/v2.1/v2.2 从描述层面修复，但根本原因是**执行顺序倒置**：AI 先生成「📝 已写入」的文字（这在 AI 内部等于「宣告完成」），工具调用被跳过。

**根因本质**：LLM 生成文字 ≠ 执行工具调用。「宣告完成」的文字一旦生成，AI 认知上已经「完成」了这个任务，工具调用被视为「可选的补充」而不是「必须的前提」。

**修改内容**：
- 修改：Step 3 → 加入「⚠️ 执行顺序铁律：工具调用→成功确认→告知用户（不可颠倒）」
- 修改：步骤顺序显式化（标注「工具调用-读取」「工具调用-写入」「确认后告知」）
- 新增：工具调用报错时的处理（输出⚠️而非📝）
- 新增：遗漏工具调用时的补救要求（不以声称代替执行）

**验证方法**：下次任务完成时，检查响应中是否有实际的 Read + StrReplace 工具调用（而非只有文字）

**验证状态**：🔵 待验证（需在下次任务完成时观察工具调用是否真实发生）

### 2026-03-20 — v2.4 → v2.5 — 新增 Signal F（用户认知输入信号，自动捕捉「表达即沉淀」飞轮）

**根因**：用户在对话中表达原则/洞见/规律性总结时，AI 没有自动捕捉机制。现有 A-E 信号只覆盖操作层（踩坑/新发现/步骤偏差/触发词/缺失Skill），不覆盖认知输入层。导致「用户说了，但没有被记录，对话结束后丢失」的高价值认知流失。

**触发场景**（真实案例，2026-03-20）：用户在梳理 AI 编程底层思维问题后，表达了「应该合理记录」「记录应该是自动化的」「要找共性、修改、留痕」「思考应自动存为碎片」等 4 条原则性洞见。这些输入在本次修复前需要用户明确说「帮我记录」才能触发 cognitive-capture-fragment。

**修改内容**：
- 新增：Signal F（用户认知输入信号）到信号定义章节，含判断标准/排除标准/记录格式/后续处理说明
- 含义：AI 在 Step 1 回顾时，若检测到用户在本次对话中有 Signal F 输入，追加到 PENDING-EXPERIENCES（类型：认知碎片）
- 与现有机制对接：project-retrospective 遇到「认知碎片」类条目时，调用 cognitive-capture-fragment 正式写入 L2
- 同步：PENDING-EXPERIENCES.md 信号类型说明中新增「认知碎片」类型描述
- 备份：history/auto-experience-hook_v2.4_20260320.mdc

**验证方法**：下次用户表达原则性洞见（如「X 应该 Y」「AI 总是 Z」等模式）时，检查 PENDING-EXPERIENCES 是否自动出现「认知碎片」条目

**验证状态**：🔵 待验证

### 2026-03-20 — v2.4 → v2.5 — 新增信号G（工具使用违规）+ 技术困难处理规范

**根因**：

本次对话中 AI 大量使用 Python 脚本（Shell工具）写入文档文件，等价于用 echo/cat 写文件——属于规范明确禁止的行为。但 auto-experience-hook 的信号 A-F 没有覆盖「工具选择违规」这类问题，导致 AI 自我判断「这是合理的技术变通」，信号从未被触发，用户也无从知情。

用户指出：「反思」的含义是「规范出了什么问题」，而不只是 AI 的自我检讨。

识别出的规范缺口（三处）：
1. **信号覆盖范围不足**：信号 A-F 只覆盖 Skill 执行偏差和认知输入，不覆盖工具使用违规
2. **技术困难处理规范缺失**：没有规定遇到技术困难时的标准流程，导致 AI 自行选择违规变通
3. **规范从手段层而非意图层定义**：「不用 echo/cat」是手段层定义，AI 可以「合理地」用 Python 绕过

**修改内容**：
- 新增：信号 G（工具使用违规信号）——判断标准：Shell/Python/PowerShell 写文件、grep/cat 搜读文件
- 新增：「技术困难时的强制规范」——禁止静默使用违规替代方案，强制三步流程（说明→检查→请示）
- 特别说明：「Python 脚本写文件」和「echo > file」意图等价，均属违规

**验证方法**：下次遇到编码/API不可用等技术困难时，AI 应说明困难 + 寻求合规替代，而非静默切换工具

**验证状态**：🔵 待验证

### 2026-03-21 — 信号 G 新增「合规例外场景」（project-retrospective E1 修复）

**根因**：信号 G 的规范对「大文件写入」一刀切禁止，但在 2026-03-21 推文任务中，生成 35MB HTML（含 24 张 base64 图片）在技术上无法用 Write 工具完成（内容超大、需要 markdown 库处理）。当前规范与现实存在不可解决的矛盾。用户在 project-retrospective 中将此识别为 E1 类架构级改进。

**识别的规范漏洞**：「任何 Python 写文件都违规」的判断标准太宽泛，无法区分「规避 Write 工具的懒行为」和「技术上合理的大文件生成」两种场景。

**修改内容**：
- 新增：信号 G「合规例外场景」条款——满足4个条件时（超过 2MB 阈值/提前声明/Write 工具写脚本/不静默）Python 写大文件不触发 G 信号
- 修改：技术困难强制规范 → 步骤2 加入「含上方合规例外场景检查」

**备份**：`history/auto-experience-hook_v2.5_20260321_before_retro.mdc`

**验证方法**：下次生成大文件时，AI 应提前声明「本任务需要 Python 生成大文件」而非静默执行，且不触发 G 信号

**验证状态**：🔵 待验证

### 2026-03-21 — Step 1.5 新增（Algedonic 时间约束，F-049）

**根因**：F-049（Beer 1972 §16.7）揭示痛-乐通道必须有时间约束，PENDING-EXPERIENCES 无超期机制导致告警通道静默截断（条目可无限期积压无任何提示）。同时，role-menu Rule 14 是真正的执行入口（alwaysApply），必须两处同步修改。

**修改内容**：Step 1 之后插入 Step 1.5「Algedonic 积压检查」：>7天→低调提示；>30天→升级告警。同步更新 role-menu Rule 14 的内联描述。

**备份**：`history/auto-experience-hook_20260321_before_p0c.mdc` 和 `history/role-menu_20260321_before_p0c.mdc`

**验证状态**：🔵 待验证

### 2026-03-21 — Step 3 追加多 session 并发序号冲突规避说明（步骤偏差C修复）

**根因**：多 session 并行工作时，各 session 各自读取末尾序号均得到相同值，导致序号冲突（实测：TASK-20260321-33 被三个 session 同时使用）。这不是 AI 操作错误，而是并发写入的固有竞争条件。Step 3 原有的「先读再写」逻辑无法感知其他 session 的并行写入。

**修改内容**：
- 修改：Step 3 第1步 → 补充「grep 全文取最大序号（不只看末尾）」说明
- 新增：「多 session 并发冲突规避」说明块——检查序号是否被占用，被占用时改用更大序号或加字母后缀

**备份**：`history/auto-experience-hook_20260321_before_b5concurrent.mdc`

**验证状态**：🔵 待验证

---

### 2026-03-23 — 废弃声明（Deprecated）

**根因**：2026-03-23 project-retrospective 扫描发现，本规则（auto-experience-hook）与 session-bootstrap.mdc 序列B的 B1/B2/B4 步骤功能完全重叠。session-bootstrap 在 2026-03-21 以步骤序列形式创建，alwaysApply:true，执行可靠性更高，实质已取代本规则。本规则是历史遗产（legacy），状态改为功能性冗余（Deprecated）。

**影响范围**：仅补充说明声明，不删除已有内容（保留追溯性），不修改 alwaysApply（已是 false）。

**修改内容**：文件头部追加废弃警告，说明正确执行入口为 session-bootstrap.mdc 序列B。

**验证状态**：🟢 已验证（session-bootstrap B1-B4 在多次会话中稳定执行）

---

### 2026-03-23 — 新增信号 G（结构性根因）+ description 更新（元反思层修复）

**根因**：A-F 信号只能感知「执行层」问题（踩坑/发现/偏差/触发词/缺失Skill/认知碎片），无法即时感知「设计层」问题——即规范/机制本身有缺陷（连接缺口/类型错误/执行力度不足）。即时感知层的盲区导致这类问题只能在批量复盘时才被发现。

**修改内容**：
- 新增：信号 G（结构性根因）——任务结束回顾时，若发现失败根因是规范机制设计缺陷，立即记录到 PENDING-EXPERIENCES（类型：结构性根因，含缺陷类型字段）
- 修改：description → A-F → A-G，加入信号G说明

**备份路径**：`history/auto-experience-hook_20260323_before_signalG.mdc`

**验证状态**：🔵 待验证
