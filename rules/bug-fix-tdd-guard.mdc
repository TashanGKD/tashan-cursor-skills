---
description: TDD 修复约束规则。当任何开发角色（后端/前端/AI工程师）正在修复一个 Bug 时，强制要求先写失败测试证明Bug存在，再写修复代码使测试通过，再运行全量CI，最后更新追踪台。禁止：没有测试的修复、合并无关代码改动、跳过CI、跳过追踪台更新。当开发角色激活 AND 任务上下文含「修复Bug」「fix」「TC-XXX」「追踪台」「BUG-」「P0/P1/P2修复」时自动激活。
alwaysApply: false
---

# TDD 修复约束（bug-fix-tdd-guard）

> **核心问题**：修复 Bug 时最容易犯的错误：
> 1. 猜测性修复（没复现就改代码）→ 假修复，Bug 还在
> 2. 没有回归测试 → 同一个 Bug 几周后复活
> 3. 借机重构 → 修一个出另一个
> 4. 修完没更新追踪台 → 协调者不知道状态变了

---

## 触发条件

以下任一条满足，且任务为开发类（修复代码）：

- 对话上下文包含「修复Bug」「fix bug」「TC-XXX」「BUG-YYYYMMDD-NN」
- 当前任务来自 bug-fix-loop-coordinator 的 dispatch
- 用户说「修复追踪台里的XX」「帮我修一下TC-XXX」
- 开发角色激活时上下文有「P0修复」「P1修复」等字样

---

## 激活后立即执行（强制序列，不可跳过任何步骤）

```
Step F1 【先复现，不要动代码】
        在本地/测试环境稳定复现该 Bug：
        - 执行追踪台中记录的复现步骤
        - 确认能稳定触发（不是偶发的flaky）
        
        IF 无法复现：
          → 不要修改任何代码
          → 更新追踪台：「复现失败，更新复现步骤需要确认」
          → 告知 bug-fix-loop-coordinator：「TC-XXX 无法复现，需要更多信息」
          → 停止

Step F2 【写失败测试（RED）——在改代码之前】
        编写能捕获到这个 Bug 的测试用例：
        - 测试必须在修复前失败（证明捕获了问题）
        - 测试粒度：尽量选单元测试或集成测试，不要写E2E（太慢，太脆）
        - 测试命名：test_fix_TC_XXX_[bug描述简写]
        
        运行测试，确认它失败：
        IF 测试通过（没有失败）：
          → 说明测试没有捕获到Bug，重新设计测试
          → 不允许进入 Step F3
          
        证明测试捕获到Bug后，继续。

Step F3 【写最小修复代码（GREEN）】
        原则（来自调研文档 TECH-008）：
        ✅ 只改让测试通过所需的最少代码
        ✅ 只在明确需要修改的地方改动
        ❌ 禁止：借机重构无关代码
        ❌ 禁止：一次修复多个Bug（每个Bug独立执行）
        ❌ 禁止：修改超出该Bug影响范围的代码
        
        写完后运行测试：
        IF 测试仍然失败：
          → 检查修复逻辑，重新分析根因
          → 最多3次尝试，仍不通过则标记「需人工介入」

Step F4 【全量CI回归（必须通过才能继续）】
        运行全量测试套件：
        - Layer 0（无LLM）：所有单元测试 + 集成测试
        - Layer 1（有LLM，时间允许时）：核心链路
        
        IF 有其他测试失败（影响半径）：
          → 分析是否是本次改动引起
          → 是 → 修复相关代码，确保不破坏已有功能
          → 不是（flaky test）→ 记录，不阻止，继续
        
        IF CI 全绿：继续 Step F5

Step F5 【更新技术问题追踪台（必须执行）】
        Read: 项目群/[项目]/技术架构师/技术问题追踪台.md
        
        将对应Bug条目的状态更新为：
        「已修复（[日期]，修复说明：[一句话说明改了什么]，TDD证据：测试文件路径）」
        
        IF 找不到对应条目：
          → 向 bug-fix-loop-coordinator 报告异常

Step F6 【向修复记录追加条目】
        Append to: 项目群/[项目]/内部总控/修复记录.md
        
        格式（见 bug-fix-loop-coordinator Step 3.5 的格式模板）：
        - Bug ID
        - 根因（一句话）
        - 修复内容
        - TDD证据（失败测试路径 + 通过证明）
        - CI状态

Step F7 【通知协调者】
        输出给 bug-fix-loop-coordinator：
        「✅ TC-XXX 修复完成
         - 失败测试：[测试路径]
         - 修复文件：[文件路径]
         - CI状态：全绿
         - 追踪台已更新
         
         等待回归测试验证。」
```

---

## 禁止行为（任何一条违反 = 本次修复无效）

```
❌ 禁止：没有写失败测试就开始改代码
❌ 禁止：测试没有失败过就认为测试捕获了Bug
❌ 禁止：CI 没有通过就提交/合并
❌ 禁止：修复 PR 中包含无关代码改动（借机重构）
❌ 禁止：同一个 PR 修复多个 Bug
❌ 禁止：修完不更新追踪台状态
❌ 禁止：修完不追加修复记录
❌ 禁止：以「时间紧」为由跳过任何步骤（P0 情况下步骤可以快，但不能跳）
```

---

## 修复失败的处理规则

```
第1次修复失败（测试不通过）：
  → 重新分析根因（5 Whys 再深一层）
  → 检查是否修改了正确的位置

第2次修复失败：
  → 查看是否有类似的历史 Bug 解决方案
    Read: .cursor/skills/role-后端开发/knowledge/后端踩坑速查.md
    或 Read: .cursor/skills/role-前端开发/knowledge/前端踩坑速查.md
  → 寻找根本原因，而不是反复试错

第3次修复失败：
  → 标记追踪台状态为「需人工介入（3次修复失败）」
  → 向 bug-fix-loop-coordinator 报告
  → 停止本 Bug 的处理，等待用户介入
```

---

## P0 Bug 的加急处理

P0 Bug（阻塞核心路径）走相同步骤，但允许压缩时间：
- 复现确认：接受「大概率能复现」（≥3/3次）
- Code Review：最少1人，10-20分钟快速 review
- CI：Layer 0 必须全绿，Layer 1 可以并行运行

**P0 不允许跳过的步骤**：F2（写失败测试）、F5（更新追踪台）

**P0 完成后强制触发 Postmortem 提醒**：
```
输出：「📝 P0 Bug [TC-XXX] 已修复。建议在24小时内完成 Postmortem，
      分析：为什么这个 Bug 没有被测试拦截？
      写入：内部总控/Postmortem-[Bug描述]-[日期].md」
```

---

## 与其他规则的关系

| 规则 | 关系 |
|---|---|
| `code-reuse-guard.mdc` | 并列：修复时若需要复用代码，code-reuse-guard 约束复用行为 |
| `reuse-not-rewrite-guard.mdc` | 并列：修复时不能「理解后重写」，应复用现有代码 |
| `comprehensive-change-guard.mdc` | 并列：修复若跨多层（产品+技术），需要多层协调 |
| `session-bootstrap.mdc` A1.7 | 本规则被 A1.7 在检测到「修复Bug」类任务时激活 |

---

## 变更记录

### v1.0 — 2026-03-24 — 初始创建

**根因**：
- 调研（TECH-008）发现：「修一个Bug出另一个Bug」的根本原因是没有TDD约束和影响半径分析
- bug-fix-loop-coordinator 需要一个强制规则确保每次修复都有测试保证
- 现有 Skill 体系中修复 Bug 时没有任何硬性约束，全靠 AI 自觉

**设计决策**：
- alwaysApply: false（只在修复上下文激活，不增加全局 context 负担）
- 7步序列不可跳过（包括P0紧急情况）
- 追踪台更新作为强制步骤（Step F5），确保协调者能感知状态
- 修复失败3次升级为「需人工介入」（防止无限重试）

**验证状态**：🔵 待验证（下次 bug-fix-loop-coordinator 触发修复时验证）

**备份路径**：无（新建文件）
