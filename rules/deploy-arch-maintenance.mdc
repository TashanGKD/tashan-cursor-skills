---
description: 部署架构文档维护规则（RULE-21/22/23/24/25）。当任务涉及部署、服务器、容器、端口、域名、CI/CD、新项目上线、技术架构设计、数据库操作时自动激活。
globs:
alwaysApply: false
---

# 部署架构文档维护规则

## ⚠️ 唯一真源声明

**所有部署、服务器、基础设施、技术架构相关任务，必须先读以下文件，再动手：**

```
_内部总控/开发规范/部署与基础设施全景手册.md
```

> `部署架构总览.md` 已于 2026-03-19 归档，不再维护，请勿读取作为操作依据。

---

## RULE-21：每个项目必须有 DEPLOY_ARCH.md

- **已部署**：如实记录当前生产环境，包含：架构图、文件分布表、端口映射、部署流程、变更记录
- **未部署**：记录「📋 计划部署架构」，标注为计划中

**图的格式**（三层）：
```
底层：物理服务器/云服务器
中间：每台服务器上的容器/进程/数据库/文件
顶层：前端（哪些页面/域名，依赖哪些后端模块）

外部依赖：用箭头指向「外部」，注明服务名称和用途
```

---

## RULE-22：部署变动时必须同步更新

**触发条件**（满足任一即触发）：
- 新增/删除服务器、容器、数据库
- 端口、域名、路径变更
- 新增/修改外部服务依赖（API、第三方服务）
- 新项目首次部署上线
- 项目下线或迁移

**必须更新的文件**：
1. 该子项目的 `DEPLOY_ARCH.md`（变更记录表追加一行）
2. `_内部总控/开发规范/部署与基础设施全景手册.md` 第一章架构图 + 第十二章对应项目快照 + 第十六章索引

**每次 infra/deploy/ci 类型的 commit 前，检查**：
```
□ 子项目 DEPLOY_ARCH.md 是否需要更新？
□ 部署与基础设施全景手册.md 是否需要更新？
```

---

## RULE-23：全局部署与基础设施全景手册结构

文件：`_内部总控/开发规范/部署与基础设施全景手册.md`

**必须包含**（维护时不可删除这些章节）：
- 第一章：当前基础设施总览图（随部署实时同步）
- 第六章：端口分配表（新项目前必须核对）
- 第十二章：各项目部署快照（新增/变更后同步）
- 第十五章：未来规划 Phase 1-4（产品决策驱动，不随日常部署更新）
- 第十六章：DEPLOY_ARCH.md 索引（新增子项目时追加）
- 附录 A：凭据速查表（凭据变更时同步）

---

## RULE-24：统一账号体系 — 禁止重复造轮子（前端 + 后端均适用）

> **核心原则**：他山所有产品共享一套账号系统。TopicLab 是账号中心，其他产品通过共享 JWT + 共享 `public.users` 表接入，**不允许在任何新项目中实现独立的认证/注册/登录模块**。

**适用范围**：一切新建项目（前端 + 后端）

---

### 前端规范

1. **登录/注册页**：直接使用与 TopicLab 相同的 `/auth/login`、`/auth/register` 接口规范，视觉照抄 TopicLab Login.tsx（仅修改 Router 依赖）
   - **注意**：注册流程可能因产品不同而异（SMS vs 邀请码），必须先验证后端接口字段，不可盲目照抄
2. **JWT Token**：存 localStorage `auth_token`，Header 格式 `Authorization: Bearer {token}`
3. **JWT Secret**：共享值 `97c24dcb8ccdd9691c795b3a1106781211caa1283f863140caac917d39029776`

### 后端规范

4. **认证实现方式**（这是本规则最核心的约束）：

   > **原则**：登录页「直接在服务器上拿来用」，不是复制代码，而是运行时代理同一个服务。

   ```
   正确架构：
   用户浏览器 → /api/auth/login → openbrain-backend
                                       ↓ 代理转发（httpx）
                                   topiclab-backend:8000/auth/login
                                       ↓ 返回 token
   openbrain-backend 附加 workspace_id → 返回给前端
   ```

   - ❌ 禁止：在新项目 backend 中实现自己的 login/register/token 验证逻辑
   - ✅ 必须：`/auth/login` 和 `/auth/me` 用 httpx 代理到 `AUTH_SERVICE_BASE_URL`（topiclab-backend:8000）
   - ✅ 允许：项目可以有 OpenBrain 特有的注册流程（邀请码），但用户创建后必须同步到 `public.users`（含所有 NOT NULL 字段，含 `handle`）
   - ✅ 允许：代理后可在响应中附加项目特有字段（如 `workspace_id`）

   **实现模板**（OpenBrain backend/main.py 已实现，新项目直接照抄）：
   ```python
   AUTH_SERVICE_BASE_URL = os.environ.get("AUTH_SERVICE_BASE_URL", "")
   
   @app.post("/auth/login")
   async def login(req):
       if AUTH_SERVICE_BASE_URL:
           async with httpx.AsyncClient() as c:
               resp = await c.post(f"{AUTH_SERVICE_BASE_URL}/auth/login", json=...)
           # 附加项目特有字段（如 workspace_id）并返回
       # else: 降级到本地实现（本地开发用）
   ```

5. **用户创建**：注册时必须在 `public.users` 写入所有 NOT NULL 字段（含 `handle`）。最安全的方式是先用 `SELECT nextval` 预取 ID，再 INSERT 含 handle 的完整行。

6. **Docker 网络**：加入 `tashan-shared` external network 才能访问 topiclab-backend
   ```yaml
   services:
     backend:
       networks: [default, tashan-shared]
   networks:
     tashan-shared:
       external: true
   ```

7. **数据库**：新项目独立 schema，`public.users` 只读引用（不复制）

---

### 新项目检查清单（进入后端开发前必须逐项确认）
```
□ 端口已在全景手册第六章端口表登记（无冲突）
□ 后端没有自己的认证模块（verify_token 直接照抄 TopicLab 两个函数）
□ 用户创建时包含 public.users 所有 NOT NULL 字段（先查 schema，再写 INSERT）
□ tashan-shared docker network 已加入
□ JWT_SECRET 使用共享值
□ 前端 auth API 路径与后端接口对齐（先 curl 验证字段，不盲目照抄 TopicLab）
□ 数据库使用独立 schema，账号引用 public.users
□ LLM 环境变量使用新标准命名：LLM_API_KEY / LLM_BASE_URL / LLM_DEFAULT_MODEL
```

---

### 违反后果

- 在新项目中创建独立 auth 模块 → 用户无法跨产品共用账号，维护两套用户数据
- INSERT public.users 时缺字段 → 500 Internal Server Error（本次 OpenBrain 注册 Bug 的根因）

> ⚠️ **端口登记是第一步**：新项目必须先在全景手册端口表查表并预留端口，再写技术架构文档，再开始开发。跳过此步骤导致的端口冲突只能靠重新配置解决（如 tashan-openbrain 占用了原分配给 scholar-match 的 8103 端口）。

---

## RULE-25：服务器操作必须写修改日志并更新全景手册

**触发条件**：任何在生产服务器（101.200.234.115）上执行的操作，包括但不限于：
- 部署新项目或更新已有项目
- 修改 nginx 配置
- 申请/更新 SSL 证书
- 新增/修改环境变量（DEPLOY_ENV）
- 修改 docker-compose 或端口分配
- 数据库 schema 变更
- 新增/修改 GitHub Secrets

**操作完成后必须执行**：

**Step 1：写服务器操作日志**
追加一行到 `_内部总控/开发规范/服务器操作日志.md`（文件不存在时先创建）：

```markdown
| 日期 | 操作类型 | 操作内容 | 影响范围 | 操作者 |
|------|---------|---------|---------|-------|
| YYYY-MM-DD | 部署/配置/证书/数据库/其他 | 一句话描述 | 受影响的项目/服务 | AI/用户名 |
```

**Step 2：更新全景手册**（根据操作类型选择需要更新的章节）：

```
端口变更      → 第六章端口表
SSL证书       → 第四章 4.3 已签发证书表
数据库schema  → 第五章 5.2 Schema分布表
项目部署状态   → 第十二章对应快照 + 第十六章索引
凭据变更      → 附录A凭据速查表（并同步 _内部总控/credentials/）
架构变动      → 第一章总览图
```

**Step 3：如有关联任务日志，在任务日志记录中标注「服务器操作：已更新全景手册 + 操作日志」**

---

## 现有 DEPLOY_ARCH.md 文件位置

见全景手册第十六章（`_内部总控/开发规范/部署与基础设施全景手册.md` → 第十六章）。

---

## 变更记录

### 2026-03-19 — v1.0 初始创建

根因：需要规范化部署架构文档的维护流程，建立 RULE-21/22/23。

### 2026-03-19 — v1.0 → v2.0 — 确立唯一真源 + 新增 RULE-24/25

**根因**：
1. 存在双真源问题：`部署架构总览.md` 与 `部署与基础设施全景手册.md` 并行，全景手册内容更完整且已自我声明为单一信源，但 Rule 仍指向总览.md。
2. 用户要求：① 所有前端兼容 TopicLab 共享账号体系 ② 服务器操作有修改日志规范。

**修改内容**：
- 修改：核心原则 → 明确唯一真源为全景手册，总览.md 归档
- 修改：RULE-22/23 → 引用目标改为全景手册
- 新增：RULE-24（前端 TopicLab 兼容要求）— 账号/网络/接口/数据库六条强制规范
- 新增：RULE-25（服务器操作修改日志）— 操作日志 + 全景手册同步两步强制操作

**验证结果**：
- 正向验证：触发部署类任务时 AI 应读全景手册（待真实场景验证）
- 负向验证：RULE-21 对子项目 DEPLOY_ARCH.md 的要求不变
- 冲突验证：RULE-24 与全景手册第十一章内容一致（不矛盾）

**验证状态**：🔵 待验证

---

## RULE-META-1：元项目导航文档维护

**触发条件**（满足任一时，必须更新 `_内部总控/元项目导航.md`）：

```
□ 新增子项目到项目群（新建子项目目录或 SKILL-INDEX 注册新子项目）
□ 现有子项目的核心文档路径发生变更（产品定义.md / 技术架构.md 改名或迁移）
□ 元项目顶层约束变更（技术栈/产品原则/规范继承路径）
□ 子项目状态变更（活跃→暂停→归档）
```

**更新操作**：在 `_内部总控/元项目导航.md` 对应节进行修改，并在「变更记录」节追加一行。

**变更记录（2026-03-21）**：新增 RULE-META-1。根因：新建了元项目导航文档（P1-A-2），需要维护规则保证文档随系统演进而更新。
