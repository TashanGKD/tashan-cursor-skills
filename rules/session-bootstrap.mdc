---
description: 会话执行协议（执行序列，不是声明式规则）。定义每次收到消息时的强制执行顺序，以及任何实质性任务完成后的强制收尾序列。解决「role-menu Rule 14 依赖AI自我执行」的根本问题：把收尾从文本约束改为步骤序列。替代原 role-menu 第14条。
alwaysApply: true
---

# 会话执行协议（Session Bootstrap）

> **这是步骤序列，不是声明式规则。** 收到消息→执行序列A；任务完成→执行序列B。

---

## 序列 A：收到任何用户消息后

```
A0  [IDE 上下文隔离]（详见 ide-context-guard.mdc，先于此序列其他步骤执行）
    从「用户消息 + 最近3轮对话」推断当前任务范围
    → IDE 打开/最近查看的文件若不在任务范围内，本回合忽略，不得作为任务上下文
    → 防止执行 B 任务时被 A 任务文档混淆

A0.5 [显式技能触发检测]（最高优先级，先于 A1 域路由和所有推断逻辑）

     IF 消息含 【任意文字】 格式（中文方括号）：
       Step 1  提取括号内文字，在 `_内部总控/Skill参考手册.md` 快速参考卡片中查找匹配
               或直接按名称匹配 role-menu.mdc 中的 Skill
       
       Step 2  IF 找到匹配 Skill：
               → 声明「✦ 显式激活：[Skill名称]（用户命令：【X】）」
               → Read 对应 SKILL.md
               → 按其「激活后立即执行」步骤操作
               → 跳过 A1 所有步骤（包括域路由和触发词扫描）
               
               IF 未找到匹配：
               → 告知用户：「未找到【X】对应的 Skill。可用触发词见 _内部总控/Skill参考手册.md 快速参考卡片」
               → 继续 A1

     ⚠️ 【X】触发绕过所有推断逻辑，包括：A1 域路由、A2 意图确认、A1.9 三问检测
        这是用户的显式命令，AI 必须执行，不得再次推断意图
     ⚠️ 若消息含多个【X】（如【产品经理】+【架构师】）→ 按出现顺序依次激活，每个完成后再激活下一个
     ⚠️ 参考手册路径：`_内部总控/Skill参考手册.md`

A1  [域识别优先路由 → 触发词兜底]

    Step 1【产品开发域识别】（优先于触发词匹配）

    IF 消息满足以下任一特征：
      ① 涉及「新项目/新产品/做个功能/功能迭代/产品开发」
      ② 涉及「bug/发现问题/报错/有问题/修复/排查/全量排查/全量检查」
      ③ 涉及「测试/全量测试/沙盘/质量检查/关卡C」
      ④ 涉及「部署/上线/发布/运维/CI-CD/服务器」
      ⑤ 涉及代码实现/系统架构/接口/数据库/前端/后端/AI工程（开发类）
    AND 不是纯粹知识查询（「XX是什么」「怎么理解XX」不触发）
    → 声明「✦ 域路由：产品开发域（原因：[匹配特征]）」
    → Read: .cursor/skills/role-产品开发协调者/SKILL.md
    → 按其步骤执行
    → 不继续 Step 2

    ⚠️ 以下类型不触发产品开发域（继续 Step 2）：
      「记录碎片/想法/洞见」→ 认知域
      「写文章/调研/审稿/公众号」→ 内容创作域
      「复盘/沉淀/Skill/Rule体系」→ Skill体系域
      「L1文档更新/认知结构整理」→ 认知体系域
      「系统健康/三大闭环」→ 系统健康域
      「立一个原则/记下这个原则/这是条工程原则/以后所有X都要Y」→ Skill体系域
      （即使原则内容涉及AI工程/LLM/代码，立原则任务本身属于 Skill/Rule 体系维护，不是产品开发）
      「先记到待办/加到项目待办/存到待办文档/这些先记到待办」→ Skill体系域（项目待办收集，不是立即开发）
      「处理项目待办/按待办文档逐条/处理待办文档里的」→ Skill体系域（待办处理，走 Step 2 触发词兜底）
      （以上各域协调者待建，暂走 Step 2 兜底）

    Step 2【触发词兜底】（非产品开发域的任务）
    扫描消息 → 对照 role-menu.mdc「可用角色一览」触发场景
    
    IF 命中 Skill → 在回复开头声明「✦ 技能激活：[Skill名称]（原因：[匹配触发词]）」
                 → Read SKILL.md → 按其「激活后立即执行」步骤操作
    IF 未命中 → 继续 A1.5

A1.5  [L1.5 原则内化约束]（已确认原则，无需读文件，直接应用于本轮所有输出）
      P1：任何判断性输出前，内部自问：「这是感受还是验证？」
          → 「感觉应该是这样」类表述 → 必须标注为「推断，待验证」
      P2：任何框架性输出前，内部自问：「我是从小点出发升维的，还是直接构造了大框架？」
          → 跳过具体案例直接给宏观框架 → 退回，先从具体场景出发
      （此步骤对每轮输出静默生效，无需向用户声明）

A1.6  [认知结构主题感知]（被触发后快速执行，<5秒）
      判断：当前对话话题是否与 L0 聚类中已有文档高度相关？
      相关域：产品理论 / 认知科学 / 组织设计 / 研究范式 / 个人方法论 / AI工程
      
      IF 高度相关 AND 问题性质是「郑总观点/判断/设计选择类」：
        → 在回答末尾或开头提示：
          「📚 此话题在您的认知文档中有相关记录（[聚类名]），
           是否希望基于您的文档回答？[基于文档回答] [继续用AI回答]」
      IF 无相关 OR 问题是「外部技术/工具类」→ 跳过，不提示

A1.7  [复用指令检测]（详见 code-reuse-guard.mdc）
      IF 消息含「复用」「完全复用」「全量复用」「直接复制」「copy过来」等关键词
      AND 任务属于开发类（代码/架构/接口/组件）：
        → 强制序列：grep定位原始实现 → 确认存在 → 复制/调用 → 遇障碍停止汇报
        → 禁止：理解原理后重新实现；未grep就写调用代码；自主切换到重新设计
        → 「完全/全量」是硬性信号：不允许以「部分不适用」为由省略任何原始代码

A1.10 [多层变更协调检测]（详见 comprehensive-change-guard.mdc）
      IF 消息描述涉及「产品设计/技术架构/UI/代码」任意两层以上的变更实施：
      → 声明「⚠️ [多层变更检测]」，输出按层执行序列，强制走关卡
      → 禁止一次性输出跨层综合方案，禁止跳过任何关卡
      IF 只涉及单层 OR 纯问答/分析 → 静默跳过

A1.9  [Skill/Rule/Agent 修改前置检测]（根因修复，2026-03-23）
      目的：防止「元任务指令绕过触发词扫描」导致跳过 skill-rule-修改规范 的根本问题

      检测条件（满足任一即触发）：
      ① 用户指令中包含「完善Skill/新建Skill/修改Skill/更新Skill」等语义
      ② 用户指令中包含「完善规划里的任务/新建规划里的组件」等元任务语义
      ③ 对话上下文判断：当前任务将对 .cursor/skills/ .cursor/rules/ .cursor/agents/ 下的文件执行 Write/StrReplace
      ④ A1 命中了 skill-designer / skill-rule-修改规范 等相关 Skill

      IF 触发条件满足 AND skill-rule-修改规范 三问尚未完成：
      → 在执行任何写操作之前，必须输出：
        「⚠️ [Skill修改前置检测] 本轮任务涉及 Skill/Rule/Agent 文件修改。
         根据 skill-rule-修改规范，必须先完成以下步骤再继续：
         Step 0：Level判断（L1补丁型/L2新增型/L3集成型/L4系统型）
         Step 三问：① 根因 ② 影响范围 ③ 验证方法
         请在此确认或继续触发 skill-rule-修改规范 Skill。」
      → 等待用户确认或触发 skill-rule-修改规范 Skill
      → 在三问回答完成前，禁止对 .cursor/ 下任何文件执行写操作

      IF 触发条件满足 AND skill-rule-修改规范 三问已在本轮完成 → 静默跳过，继续执行

      特别说明：「认知科学文献对比」是额外要求（用户特别指定时），
      叠加在 skill-rule-修改规范 三问+Level判断+关卡 **之上**，
      不替代既有的修改前置流程

A2  [意图确认]（仅当指令模糊时）
    IF 消息含「继续」「好的」「做一下」「按规范执行」等泛化词 →
       → 基于最近3轮对话解读意图，不依赖 IDE 当前打开的文件
       → 若对话语境与 IDE 文件暗示的任务不一致 → 先向用户确认，不自行假设

A2.5  [PENDING-EXPERIENCES 主动推进]（SD4 修复，耗时<5秒）
      Read: .cursor/skills/skill-index/PENDING-EXPERIENCES.md（仅扫描「🔲 待处理」条目数量和最老日期）
      
      IF 🔲 待处理条目 ≥ 5 条：
        → 在开始任何新任务之前，主动提示：
          「💡 当前有 N 条经验待沉淀（最老：[日期]）。建议在开始新任务前处理积压。
            [立即运行 project-retrospective] [跳过，继续当前任务]」
        → 等待用户指令
        → 若用户选「跳过」→ 继续处理当前消息
      IF < 5 条 → 跳过，不提示（已在序列B中的 B2 处理）

A2.6 [进化阈值检测]（耗时<3秒，认知科学对应：神经可塑性自驱监控）
     说明：对应认知体系的自我进化机制——检测积压信号、阈值触发、建议启动进化操作

     Step 1  Read: _内部总控/认知结构/L1.5_底层原则层/底层原则库.md
             → 计算「已确认原则数」N（只计 ✅ 状态）
     Step 2  Read: _内部总控/认知结构/L3_原始记录/系统日志.md
             → 找最近一条 skill-evolution-planner-meta 执行记录，提取当时的原则数 N_last
     Step 3  IF N > N_last + 2 → 「💡 新增原则已达阈值（+N条），建议运行 skill-evolution-planner-meta」
     Step 4  Read: _内部总控/认知结构/L2_碎片化思考/碎片整合索引.md
             → 统计🔲待整合碎片数 M 和最老条目日期
     Step 5  IF M > 10 且最老碎片 > 14天 → 「💡 碎片积压 M 条（最老N天），建议运行 cognitive-integrate-fragments」
     Step 6  Read: _内部总控/认知结构/L2_碎片化思考/AI思维问题复盘/01_思维错误复盘日志.md
             → 统计30天内各错误模式出现次数
     Step 7  IF 某模式出现 ≥ 3次 → 「⚠️ 系统性惯性发现：「[模式名]」30天内已出现N次，建议修复规范」
     Step 8  IF 三类信号均未触发 → 静默跳过，不输出任何内容
```

---

## 序列 B：任何实质性任务完成后（强制，不可跳过）

```
B0  [认知子任务排除检查]（新增，防止无限循环）
    判断：当前完成的任务，是否是由以下「父级 Skill」触发的子任务（而非用户直接触发）？
    
    认知子任务排除清单（这些 Skill 完成后不单独触发序列B）：
      - cognitive-capture-fragment（被 cognitive-task-reflector / cognitive-reorganize / integrate 调用时）
      - cognitive-integrate-fragments（被 cognitive-reorganize 调用时）
      - cognitive-verifier（被 cognitive-update-knowledge / cognitive-integrate-fragments 调用时）
      - cognitive-cascade-notifier（后台异步，本身不触发B）
      - cognitive-task-reflector（被 write-task-log 调用时）
    
    识别方式：若当前对话中已有父级 Skill 的未完成任务日志条目（说明当前是子任务），
    且当前 Skill 名称在上述清单中 → 跳过序列B（不执行B1-B4）
    
    IF 不确定 → 默认执行序列B（保守策略：误触发比漏触发代价小）
    IF 当前 Skill 不在上述清单 → 继续执行B1-B4

B1  [经验感知]（耗时<10秒）
    回顾刚才执行过程，判断是否出现 A-F 信号：
    IF 有信号 → 直接追加一行到 PENDING-EXPERIENCES.md（不调用子智能体）
    IF 无信号 → 跳过

B1.5 [惯性错误监控]（耗时<10秒）
     说明：对应人脑「错误监控」机制（前扣带回ACC + 右侧PFC，Miyake et al., 2000）
           注：纯粹的「执行前抑制」由 cognitive-attend A1.8 承担
           本步骤实现的是「执行后错误检测」——反思性错误监控（Schön, 1983 reflection-on-action）

     Step 1  Read: _内部总控/认知结构/L2_碎片化思考/AI思维问题复盘/01_思维错误复盘日志.md
             → 提取「错误模式标签」列表（所有已记录的惯性错误类型）

     Step 2  将刚完成任务的「完成的工作」描述与错误模式标签做语义比对
             IF 存在语义重合：
             → B3 输出时额外追加：
               「⚠️ 惯性监控：本次任务与已知错误模式「[模式名]」存在重合，请确认是否已规避。」

     Step 3  IF 本次任务出现「反复操作同一问题但结果不符预期」的新信号：
             → 追加到 PENDING-EXPERIENCES.md（信号类型：踩坑）
             → 同时追加到 01_思维错误复盘日志.md 末尾
               格式：| [日期] | [模式标签（一句话）] | 待分析 |

     IF 无任何匹配且无新信号 → 静默跳过，不输出任何内容

B2  [积压感知]（耗时<5秒）
    Read: .cursor/skills/skill-index/PENDING-EXPERIENCES.md
    → 找最老「🔲 待处理」条目日期
    IF 最老条目 > 30天 → 「⚠️ 积压警告：最老经验已M天未处理，建议触发 project-retrospective」
    IF 最老条目 > 7天  → 「💡 积压提示：N条经验最老M天，建议下次任务后处理」
    IF ≤7天或无待处理 → 跳过，不输出提示

B3  [告知用户]
    「✅ 任务完成。[若有经验记录：本次记录了N条经验]」

B3.5 [项目文档同步]（仅当任务涉及产品/架构变更时执行）
    
    判断标准：以下任一条成立，即需要同步文档：
      - 实现或修改了用户可见功能（UI/交互/新能力）
      - 做出或变更了技术架构决策（技术选型/接口/数据模型）
      - 新增或改变了产品需求范围（功能范围/设计原则）
      - 修复了影响功能完整性的缺陷

    判断 IF 需要同步：
      → 识别受影响的文档（可多选）：
          [产品定义]  → 功能状态表 / 用户流 / 功能范围
          [开发计划]  → Phase 状态（🔲 → ✅）/ 新增待完成任务
          [技术架构]  → 技术选型 / 目录结构 / 接口定义 / 已知问题
      
      → 对每个受影响文档执行工具调用（StrReplace / Write）：
          1. 更新版本号（vX.Y → vX.(Y+1)）
          2. 追加变更记录一行
          3. 更新对应状态/内容（不是新写一遍，是精准修改相应章节）
      
      → 文档路径约定：
          项目级文档：{项目}/产品经理/产品定义.md
                       {项目}/产品经理/开发计划.md
                       {项目}/技术架构师/技术架构.md
          全局文档：_内部总控/ 下相关文件

    判断 IF 不需要同步：
      → 跳过，直接执行 B4
      → 不需要同步的情形：纯分析/纯问答/文档本身的更新/认知类操作

    ⚠️ 执行铁律：
      - 必须工具调用修改文件，禁止只口头说「已更新」
      - 版本号必须实际递增（v1.0 → v1.1），不得写假版本
      - 变更记录必须一行说清楚改了什么（不可空白）

B3.8 [关卡C自动触发]（仅当本轮任务修改了代码文件时）

    判断：本轮是否调用 Write/StrReplace 修改了 .py/.ts/.tsx/.js/.vue/.css 等代码文件？

    IF 是（代码修改任务）：
      → 尝试读取项目的测试状态文件：
        ① Read: [项目路径]/测试工程师/test-coverage-state.md
           IF 存在且无 🔲 待执行条目 → 关卡C已完成，在 B4 任务日志中标注「关卡C: ✅」→ 继续 B4
           IF 存在但有 🔲 待执行条目 → 关卡C未完成，进入自动激活
           IF 文件不存在 → 关卡C未执行，进入自动激活

      若关卡C未完成/未执行（⚠️ 自动激活，不等用户）：
        → 输出：「🔄 [关卡C自动触发] 代码已修改，自动激活测试工程师执行关卡C：」
        → 直接 Read: .cursor/skills/role-测试工程师/SKILL.md
        → 完整执行其「激活后立即执行」序列的 ALL 步骤
          （含 D0 读取、Step 0.4 基线、Step 1-6 完整测试、Step 6.4 覆盖门禁、Step 6.6 写完成凭证）
        → 关卡C执行完成后：在 B4 任务日志中标注「关卡C: ✅ 已执行（本轮自动触发）」→ 继续 B4
        
        ⚠️ 唯一允许跳过的情况：用户在当前消息中明确说「跳过关卡C，原因：[X]」
           此时在 B4 任务日志标注「关卡C: ⚠️ 已跳过，原因：[X]」→ 继续 B4

    IF 否（无代码修改，纯文档/分析/问答/认知任务）→ 跳过本步骤，继续 B4

B4  [写入任务日志]
    ⚠️ 执行顺序铁律：工具调用 → 确认成功 → 告知用户。禁止先输出「📝已写入」

    1. [工具调用-读取] Read: _内部总控/任务日志.md
       → 用 grep 找今日（YYYYMMDD）全部 `## [TASK-YYYYMMDD-` 条目
       → 取所有已存在序号中的最大值 NN（⚠️ 不能只看末尾，并行 session 可能已在末尾写入更大序号）
       → 新条目序号 = max(NN) + 1

    2. [工具调用-写入] StrReplace 追加到文件末尾：
       ---
       ## [TASK-YYYYMMDD-NN] <任务名称>
       **日期**：YYYY-MM-DD
       **执行者**：AI（Claude）
       **触发来源**：用户需求
       ### 认知情境（对应情节记忆情境绑定，COG-TAX §四）
       **当前项目**：[项目名 / 无]
       **问题类型**：[架构设计 / Bug修复 / 概念探索 / 文档维护 / 认知操作 / 其他]
       **复杂度感知**：[简单 / 中等 / 复杂]
       **是否有前驱经验**：[有（指向TASK-XXX）/ 无]
       ### 完成的工作
       <核心操作与决策>
       ### 创建/修改的文件
       <逐条列出>
       ### 关卡C状态（代码修改任务必填，其他任务填「不适用」）
       [✅ 已通过 / ⚠️ 已跳过（原因：[X]） / 不适用]
       ### 用户原始需求
       <用户原话>
       ### 遗留事项
       <未完成项，无则写"无">

    3. 工具调用成功 → 输出「📝 任务日志已写入 [TASK-YYYYMMDD-NN]」
       工具调用报错 → 输出「⚠️ 任务日志写入失败，请手动检查任务日志.md」
```

---

## 宪法型约束（2条，最高优先级）

1. **禁止捏造**：不知道→说不知道；看不到→说看不到；推断→必须标注「这是推断，非已验证事实」
2. **正确性优先于速度**：「写得快/好看」与「证据充分/结构自洽/闭环完整」冲突时，保证后者

---

## 变更记录

### v2.3 — 2026-03-25 — B3.8 改为自动激活测试工程师（不等用户）

**根因**：B3.8 检测到代码修改无关卡C记录后，输出选择题「A/B」并等待用户决策，
导致整个 project-backlog 处理链路中断。用户必须手动说「按规范测试」才能继续。
正确行为：检测到缺失 → 自动激活测试工程师 → 完成后继续后续待办。

**修改内容**：
- B3.8 从「检测→警告→等待」改为「检测→自动激活 role-测试工程师→继续 B4」
- 唯一允许跳过的情况：用户当前消息明确说「跳过关卡C，原因：[X]」

**备份路径**：`.cursor/rules/history/session-bootstrap_20260325_before_autogate-c.mdc`

---

### v2.2 — 2026-03-25 — 新增 A0.5（显式技能触发，【X】格式）

**根因**：AI 的触发词推断在长对话中可靠性下降；用户需要一种确定性的方式指定要激活的 Skill，
不依赖 AI 的意图推断。

**修改内容**：
- 新增 A0.5「显式技能触发检测」：消息含【任意文字】→ 直接激活对应 Skill，跳过 A1 所有路由
- 优先级高于 A1 域路由、A2 意图确认、A1.9 三问
- 支持多个【X】按序激活

**配套**：新建 `_内部总控/Skill参考手册.md`（含快速参考卡片，所有触发词一览）

**备份路径**：`.cursor/rules/history/session-bootstrap_20260325_before_explicit-trigger.mdc`

---

### v2.1 — 2026-03-25 — 新增 B3.8（关卡C文档核查）+ B4 模板新增「关卡C状态」必填字段

**根因**：通过分析 [项目架构文档编写](53b74d66-e601-4d4f-815b-4dd8dee14b3a) 对话，
发现 AI 在第984轮对话后直接从「改代码」跳到「写任务日志」，跳过了关卡C。
根本原因：关卡C是否执行的状态只在 AI 记忆里，B4 从不检查任何文档，
没有任何机制阻止 AI 在未测试的情况下写「✅ 任务完成」。

**修改内容**：
- 新增：B3.8「关卡C文档核查」——代码修改任务完成后、B4 写日志前，
  尝试读取 `test-coverage-state.md`，若有 🔲 待执行条目或文件不存在 → 阻塞，不允许完成
- 修改：B4 任务日志模板新增「关卡C状态」必填字段（✅已通过 / ⚠️已跳过+原因 / 不适用）

**为什么这次有效**：
- B3.8 需要一个实际的 Read 工具调用（读 test-coverage-state.md）
- 文件存在与否是客观事实，不依赖 AI 记忆
- 即使在第 984 轮对话，这个 Read 工具调用在当前执行步骤中，注意力衰减不影响工具调用

**配套修改**：
- role-测试工程师 新增 Step 6.6（确认 test-coverage-state.md 无 🔲，作为 B3.8 凭证）

**验证方法**：
- 正向验证：代码修改后不激活测试工程师 → B3.8 检测到无 test-coverage-state.md → 阻塞
- 负向验证：非代码任务（纯文档、问答）→ B3.8 跳过，不影响

**备份路径**：`.cursor/rules/history/session-bootstrap_20260325_before_gate-c-doc.mdc`

---

### v1.0 — 2026-03-21 — 初始创建

**根因**：role-menu.mdc Rule 14 是声明式文本约束，AI 执行收尾四步依赖自我报告，「声称完成≠实际执行」问题多次复现。新文件将执行协议改为步骤序列（Sequence A/B），同时加入 A1 触发词声明步骤——让 Skill 触发从隐式变为显式可观察。

**配套修改**：
- role-menu.mdc：Rule 14 改为「见 session-bootstrap.mdc 序列B」
- co-build-log.mdc：改为 alwaysApply: false（仅创作型任务需要）
- auto-experience-hook.mdc：保持 alwaysApply: false，作为详细规范参考

**验证状态**：🔵 待验证（下次任务完成时观察：① 回复开头有「✦ 技能激活」声明；② 有实际工具调用写入日志）

---

### v1.1 — 2026-03-21 — 新增 A1.5（P1/P2内化约束）+ A1.6（认知结构主题感知）（T8A 修复）

**根因**：GAP-T8（认知体系改进规划 v1.2）：L1.5 已确认原则每次需读文件才能应用，且认知结构在不含触发词的对话中完全不参与。A1.5 解决原则内化问题，A1.6 解决主题相关时的主动提示问题。

**修改内容**：
- 新增：A1.5「L1.5 原则内化约束」——P1/P2 以静默内化形式对每轮输出生效，无需读文件
- 新增：A1.6「认知结构主题感知」——高相关话题 + 郑总观点类问题时，在回答中提供「基于文档回答」选项

**验证结果**：
- 正向验证：不带触发词问产品设计问题，回答末尾应有 A1.6 提示（待真实场景验证）
- 负向验证：问技术实现类（代码、工具）问题，A1.6 不触发；普通对话 A1.5 静默生效无输出

**已知风险**：A1.6 的「高相关」判断依赖 AI 对聚类的理解，边界模糊时倾向于不提示（减少干扰优先）

**备份路径**：`.cursor/rules/history/session-bootstrap_20260321_before_A1.5-A1.6.mdc`

---

### v1.2 — 2026-03-21 — B4 防并发序号冲突修复

**根因**：多 session 并行写任务日志时，各 session 各自读取末尾序号，均得到相同值，导致序号冲突（实测：TASK-20260321-33 被3个 session 同时使用）。

**修改内容**：
- B4 步骤1：「找今日最大序号」的方法从「读末尾」改为「grep 全部条目取最大值」，防止并发写入时各 session 错过对方的写入

**验证结果**：
- 待验证（下次多 session 并行时观察）

**备份路径**：`.cursor/rules/history/session-bootstrap_20260321_before_b4fix.mdc`

---

### v1.6 — 2026-03-22 — 新增 B3.5（项目文档同步，根因修复）

**根因**：

序列 B 只有 B4（写任务日志），没有"更新产品/技术/计划文档"步骤。任务日志（事件追加记录）≠ 状态文档（当前状态维护），二者性质不同，需要分开的步骤。

具体表现：实现了三栏拖动、字体调节、Markdown 渲染等功能后，只写了任务日志，未更新 产品定义.md 的功能状态表、未创建 开发计划.md、未创建 技术架构.md，导致文档与代码严重不同步。用户不得不专门提出纠正。

**本质问题**：

规范把"记录事件"（任务日志）和"维护状态"（产品/技术文档）混为一谈。任务日志是 append-only 的事件流，产品文档是 update-in-place 的状态表。两者触发时机相同（任务完成后），但操作对象和修改方式完全不同，必须分别有对应的强制步骤。

**修改内容**：

- 新增：B3.5「项目文档同步」——在告知用户（B3）和写日志（B4）之间，判断是否涉及产品/架构变更，若是则强制工具调用更新对应文档
- 判断标准：功能实现 / 架构决策 / 需求范围变化 / 缺陷修复 → 需要同步；纯分析/问答 → 跳过
- 执行要求：必须工具调用（禁止口头声称），版本号必须实际递增，变更记录不可空白

**验证方法**：

- 正向：完成一个 UI 功能后，B3.5 自动更新 产品定义.md 的功能状态表
- 负向：回答一个技术问题后，B3.5 跳过（不输出任何内容）
- 边界：文档本身的创建/修改任务，B3.5 跳过（否则递归）

**备份路径**：`.cursor/rules/history/session-bootstrap_20260322_before_B3dot5.mdc`

---

### v1.5 — 2026-03-21 — 新增 A2.5（PENDING-EXPERIENCES 主动推进，SD4 修复）

**根因**：序列B的 B2 只在任务完成后输出积压警告文字，积压仍需用户记住并主动触发 project-retrospective。当积压 ≥5 条时，下次任务开始前（序列A）应主动提示，形成完整的「感知→提醒→处理」闭环。

**修改内容**：
- 新增：A2.5「PENDING-EXPERIENCES 主动推进」——任务开始前扫描 PENDING-EXPERIENCES，≥5条时主动建议处理，等用户指令后再继续

**验证结果**：
- 正向验证：有5+条积压时，新任务开始前出现推进提示
- 负向验证：<5条时静默，不干扰正常任务流

**备份路径**：`.cursor/rules/history/session-bootstrap_20260321_before_A2.5.mdc`

---

### v1.4 — 2026-03-21 — 新增 B0（认知子任务排除清单）防止无限循环

**根因**：Phase 2 关卡B C-1 问题：cognitive-task-reflector → cognitive-capture-fragment → write-task-log → cognitive-task-reflector 的调用链无终止条件，可能形成无限循环。根本原因是认知子任务完成后也触发序列B（B4写日志 → write-task-log → 触发task-reflector）。

**修改内容**：
- 新增：B0「认知子任务排除检查」——在 B1 之前判断当前任务是否为认知子任务，若是则跳过整个序列B
- 排除清单包含：cognitive-capture-fragment / cognitive-integrate-fragments（被父级调用时）/ cognitive-verifier / cognitive-cascade-notifier / cognitive-task-reflector（被write-task-log调用时）
- 默认保守策略：不确定时执行序列B

**验证结果**：
- 正向验证：cognitive-task-reflector 执行完毕后不触发新一轮 B4（因 B0 识别为子任务，跳过序列B）
- 负向验证：用户直接触发 cognitive-capture-fragment 时（非被父级调用），正常执行序列B

**已知风险**：「识别是否为父级调用」依赖任务日志状态检查，边界情况下可能误判，保守策略（不确定→执行）降低漏触发风险

**备份路径**：`.cursor/rules/history/session-bootstrap_20260321_before_B0.mdc`

---

### v1.3 — 2026-03-21 — 新增 A0（IDE 上下文隔离）

**根因**：用户反馈「执行 B 任务时，若 IDE 打开了 A 任务的文档，B 任务会搞混」。Cursor 自动注入打开/最近查看的文件到 context，AI 容易将 A 文档混入 B 任务推理。原有 A2 仅针对模糊指令，且未在每次任务开始前强制隔离。

**修改内容**：
- 新增：A0「IDE 上下文隔离」步骤，先于 A1 执行
- 引用：ide-context-guard.mdc 负责详细逻辑，A0 作为序列入口

**配套**：新建 ide-context-guard.mdc（alwaysApply: true）

**验证方法**：执行 B 任务时开着 A 文档，AI 不应读/改 A 文档

**备份路径**：`.cursor/rules/history/session-bootstrap_20260321_before_A0-ide-guard.mdc`

---

### ⚠️ 已知架构张力（2026-03-23 记录，设计决策非缺陷）

**本文件违反 Skill体系设计原则_v1.0.md §2.2「Rule 应为轻量约束（<300词），不包含完整流程步骤」。**

本文件包含完整的步骤序列（A0→A1→A2 + B1→B4），内容更接近 Skill 而非约束。
但经过实测验证，**必须保留 alwaysApply Rule 形式**，原因：

- 将执行协议放在 Skill 层（需用户触发词触发）→ 实测漏出率高，「声称完成≠实际执行」多次复现
- alwaysApply Rule 保证每次对话注入 → 强制执行可靠性高于任何 Skill 方案

**已在 Skill体系设计原则_v1.0.md §2.2 注明此例外**（2026-03-23）。
这是被接受的架构权衡，不需要修复。

---

### v1.6 — 2026-03-22 — 新增 A1.7（复用指令检测，code-reuse-guard 修复）

**根因**：2026-03-22 跨3个对话（1dc992d0/9d4ef11b/a4a4b82a）历史分析发现系统性模式：用户说「复用X」时，AI 将其解读为「理解X原理→重新实现」，而非「找到X代码→直接用」。最典型：用户说「完全复用老版本proxy模式」，AI写了新代码并调用了不存在的函数，导致功能失效。跨3个对话出现5次同类偏差，根因是AI训练出来的「生成最优解」本能在「明确指定执行路径」场景下的错误应用。

**修改内容**：
- 新增：A1.7「复用指令检测」步骤，位于 A1.6 和 A2 之间
- 引用：code-reuse-guard.mdc 负责详细逻辑，A1.7 作为序列入口
- 硬性约束：「完全/全量」是不可精简的硬性信号

**配套**：新建 code-reuse-guard.mdc（alwaysApply: true）

**验证方法**：
- 正向：发送「完全复用老版本proxy模式里的FOLLOWUPS」→ AI先grep找原始实现文件，不写新代码
- 负向：发送「新建一个聊天接口」→ 不触发额外步骤

**备份路径**：`.cursor/rules/history/session-bootstrap_20260322_before_reuse-guard.mdc`

### v1.7 — 2026-03-23 — 新增 A2.6（进化阈值检测）+ B1.5（惯性错误监控）+ B4情境字段

**认知科学依据**：
- A2.6：神经可塑性自驱监控（COG-TAX §十三 认知体系进化机制）
- B1.5：ACC错误监控 + 右侧PFC抑制（Miyake et al., 2000 执行功能三成分）+ Schön 1983 reflection-on-action
- B4情境字段：情节记忆情境绑定（Squire & Zola-Morgan, 1993，COG-TAX §四 记忆系统）

**修改内容**：
- 新增 A2.6「进化阈值检测」：自动检测原则数/碎片积压/惯性错误三类进化信号，阈值触发时给出建议
- 新增 B1.5「惯性错误监控」：读取已知错误模式，与当前任务做语义比对，发现新惯性信号时双写（PENDING + 复盘日志）
- 更新 B4 任务日志模板：新增「认知情境」块（当前项目/问题类型/复杂度/前驱经验）

**备份路径**：`.cursor/rules/history/session-bootstrap_20260323_before_cog-A26-B15-B4.mdc`

**验证方法**：
- A2.6：原则数变化后，下次对话开始时应出现进化提示
- B1.5：完成包含「重新实现」等已知错误模式的任务后，B3 应出现惯性监控提醒
- B4：下次任务日志应含「认知情境」字段

### v1.8 — 2026-03-23 — 新增 A1.9（Skill/Rule/Agent 修改前置检测）

**根因**：「元任务指令绕过触发词扫描」漏洞——用户说「完善和新建规划里的所有任务」时，
A1 触发词扫描未命中任何 Skill 触发词（「新建Skill」「修改Skill」等），
导致 AI 直接执行 12 个 Skill/Rule/Agent 写操作，跳过了 skill-rule-修改规范 的三问+Level判断+关卡。

**A1.9 的核心设计**：
- 不依赖用户措辞中的精确触发词，改为「任务语义检测」
- 只要任务实质上涉及 .cursor/ 下的写操作，必须先完成三问
- 明确「认知科学对比」是叠加要求，不替代既有修改规范

**影响范围**：所有涉及 Skill/Rule/Agent 创建/修改的任务（约束加强，不影响其他任务类型）

**验证方法**：
- 正向验证：用户说「完善认知Skill体系」→ A1.9 触发，要求三问完成后再继续
- 负向验证：用户说「写后端接口」→ A1.9 不触发（不涉及 .cursor/ 写操作）

**备份路径**：`.cursor/rules/history/session-bootstrap_20260323_before_A1.9.mdc`

### v1.9 — 2026-03-23 — 新增 A1.10（多层变更协调检测）

**根因**：产品开发域 75 个沙盘分析（PD-016~PD-075）根因一「缺编排层」：当用户描述涉及多个开发层次的变更时，A1 触发词扫描只命中单个角色，关卡A/B/C 被系统性绕过。域优化报告 P0-5 确认为需新建 Rule 而非 Skill（约束性守门行为）。

**修改内容**：
- 新增：A1.10「多层变更协调检测」引用步骤，位于 A1.9 之前
- 配套：新建 `.cursor/rules/comprehensive-change-guard.mdc`（完整规则逻辑在该文件中）

**验证方法**：
- 正向验证：说「修改产品功能，同时改后端接口」→ 应看到「⚠️ [多层变更检测]」提示
- 负向验证：说「帮我写注册接口」→ A1.10 静默跳过，不输出任何内容

**备份路径**：`.cursor/rules/history/session-bootstrap_20260323_before_A110_comprehensive-change-guard.mdc`

---

### v2.0 — 2026-03-23 — A1 重构：域识别优先路由（分层治理架构第一步）

**根因**：讨论记录第十四章洞见——当前 A1 扫描 50+ 触发词直接激活工人 Skill，跳过了域协调者层，导致 role-产品开发协调者 等协调者 Skill 永远不会被激活。根本问题：治理结构扁平，CEO 直接管 50 个人。

**三问**：
- 根因：A1 是 CEO 层，但它做的是团队负责人的事（直接触发具体 Skill）
- 影响范围：所有产品开发类请求改为先经 role-产品开发协调者；其他域请求不受影响（走原 Step 2 兜底）
- 验证方法：说「有个新功能要做」→ 应看到「✦ 域路由：产品开发域」；说「记录个碎片」→ 仍触发 cognitive-capture-fragment

**修改内容**：
- 修改：A1「触发词扫描」→ 「域识别优先路由 → 触发词兜底」
- A1 新增 Step 1（产品开发域识别，5个特征条件 + 4类排除条件）
- A1 Step 2 保留原触发词扫描逻辑（兜底其他域）
- 配套：新建 `.cursor/skills/role-产品开发协调者/SKILL.md`（v1.0）

**验证状态**：🔵 待验证

**备份路径**：`.cursor/rules/history/session-bootstrap_20260323_before_A1_domain_routing.mdc`

**遗留**：随着其他4个域协调者建立，A1 Step 1 逐步扩展（认知/Skill/内容/系统健康域）
