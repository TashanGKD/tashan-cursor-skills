---
name: research-output
description: 对任何主题进行系统调研，自动产出图文并茂的 Markdown 文档（含 Mermaid 结构图 + qwen-image 可视化），保存到认知结构对应维度的知识库，并自动注册到文档分类清单和知识图谱。当用户说「帮我调研/调研一下/系统调研/研究一下/帮我系统了解/深度研究 [主题]」时触发。
---

# 系统调研输出 Skill（research-output）

> 触发词：「调研」「系统调研」「研究一下」「帮我系统了解」「深度研究」+ 主题词
> 产出：REF-EXT 图文 Markdown → 保存到认知结构知识库 → 自动注册三处
> ⚠️ Steps 1-3 均为内存操作，只在 Step 4 执行一次性文件写入

---

## ⚡ thin wrapper 转发（优先执行）

> 本 Skill 已升级为 document-pipeline 的入口。触发后立即转发，不独立执行。

```
触发后立即执行：
  设置 target_format = md
  设置 mode = research
  转发到 document-pipeline（加载其 SKILL.md，从 Stage 0 开始执行）
  
  说明：调研逻辑已迁移到 document-pipeline，包括：
    - 网络调研 + 草稿撰写（Stage 1）
    - Mermaid + qwen-image 图文生成（Stage 4-5）
    - 保存到知识库 + 三处注册（Stage 8D）
```

---

## 触发判断（先执行）

```
IF 用户说「帮我调研/大量调研/全景调研/全面调研/研究一下/调研一下/系统了解/深入了解/搜一下/查一下（需要结构化知识产出时）」
AND 有明确的调研对象（不是临时问一个问题/不是空答）
→ 触发本 Skill（mode=research，从 Step 0 开始）

IF 用户说「完善[已有文档]/给[已有文档]加图/补充[已有文档]/这个文档缺图/按规范完善」
AND 明确指向某个已存在的知识库文档（REF-EXT）
→ 触发本 Skill（mode=improve，从 Step 2 开始）
   Step 2：读取已有文档，识别适合 qwen-image 的位置
   Step 3：生成图片并嵌入
   Step 4：更新文件（替换原文件，而非新建）
   Step 5：跳过（已注册，无需重复）

不触发（优先判断）：
- 「搜索一下/查一下/找一下」→ 临时查找，直接回答，不触发
- 「解释一下/这个是什么意思」→ 单点解释，不触发
- 「帮我调试/排查错误」→ 不触发
- 「研究一下/帮我研究」+ 后接「报错/错误/bug/为什么/怎么解决/代码问题/调试/排查」→ 调试场景，不触发

主题词不明确时：询问「你想调研的具体主题是什么？」等用户回答后触发。

组合场景：若用户同时含写作类关键词（「调研X然后写成文章/推文」）
→ 告知「本 Skill 先产出调研知识库文档，文章写作请完成本 Skill 后另触发 general-article-writer/wechat-article-writer」
→ 先执行本 Skill 完整流程
```

---

## Step 0：维度判断（确定知识库路径）

将调研主题映射到 L1 维度：

| 主题特征 | 目标维度 | 知识库路径（完整）|
|---|---|---|
| 技术框架/开源工具/架构/编程/API/系统设计 | 技术架构思维维度 | `_内部总控/认知结构/L1_系统性文档/技术架构思维维度/知识库/` |
| 认知心理学/脑科学/学习/记忆/认知偏误 | 认知科学基础维度 | `_内部总控/认知结构/L1_系统性文档/认知科学基础维度/知识库/` |
| 产品设计/市场/竞品/商业模式/用户研究 | 产品理论维度 | `_内部总控/认知结构/L1_系统性文档/产品理论维度/知识库/` |
| 写作/发布/内容创作/配图/工具使用方法 | 待建维度 | `_内部总控/认知结构/L1_系统性文档/待建维度/知识库/` |
| 研究方法/学术规范/论文写作/数字人/仿真 | 研究范式维度 | `_内部总控/认知结构/L1_系统性文档/研究范式维度/知识库/` |
| 组织设计/团队协作/AI组织/社区运营 | 组织理论维度 | `_内部总控/认知结构/L1_系统性文档/组织理论维度/知识库/` |

**跨维度主题**：选更具体的维度，告知用户。
**无法判断**：询问「这个主题属于哪个方向？[列出上表6个选项]」，等用户确认后继续。

---

## Step 1：网络调研 + 撰写 V1 草稿（内存操作，含 Mermaid）

**调研工具**：
```
使用 WebSearch 工具，每个核心子问题至少查 2-3 个来源。
对重要官方文档/技术文章，使用 WebFetch 读取原文。
优先：官方文档 > 学术论文 > 权威技术博客 > 其他。
```

**V1 文档结构（在内存中组织）**：

```markdown
# [主题] 调研报告

> [占位，Step 4 写入完整 REF-EXT 头部]

## 摘要
[核心结论 3-5 条，每条一句话]

## 目录

## 一、[章节标题]
[内容，关键数据标注来源]

...各章节...

## 参考资料

> 强制要求：凡通过 WebSearch / WebFetch 获取的内容，必须在此节列出来源

格式（每条必须包含全部字段）：
- [来源N] 作者/机构 | 标题 | URL | 访问日期（YYYY-MM-DD）

示例：
- [来源1] GitHub/sst | OpenCode README | https://github.com/sst/opencode | 2026-03-19
- [来源2] different-ai | OpenWork VISION.md | https://github.com/different-ai/openwork | 2026-03-19

无法找到原始 URL 的内容须标注 [来源不确定] 并写明获取方式。
```

**Mermaid 插入规则**：
```
✅ 用 Mermaid 的场景：
  - 流程/决策/步骤序列 → flowchart TD
  - 系统模块关系/层级架构 → graph LR
  - 时序交互 → sequenceDiagram

❌ 不用 Mermaid 的场景（留给 qwen-image）：
  - 数据/统计图表
  - 概念框架/矩阵/象限图
  - 需要视觉层次感的分层架构图
```

**来源缺失时**：标注 `[⚠️ 该部分缺乏可信来源]`，不编造内容。

> ⚠️ **草稿完成检查点（强制）**：V1 草稿撰写完成后，**下一步必须执行 Step 2（绘图指南检查）**，不允许直接跳到 Step 4 写文件。Step 2 是必经检查点，即使调研内容很长、文字已写完，也不可跳过。

---

## Step 2：自审 — 识别 qwen-image 画图位置（内存操作）

```
第一步：确认绘图指南文件位置
  尝试 Read: _内部总控/认知结构/L1_系统性文档/待建维度/知识库/全场景绘图指南.md
  若文件不存在（路径可能变更）：
    使用 Glob 搜索：**/全场景绘图指南.md
    找到 → 读取找到的路径
    未找到 → 跳过 Step 2 和 Step 3，告知用户「绘图指南文件未找到，已跳过 qwen-image 可视化，保存文字+Mermaid 版本」

按以下标准扫描 V1 草稿，识别适合 qwen-image 的位置：

✅ 适合 qwen-image：
  - 概念框架图（多概念关系，Mermaid 无法美观展示）
  - 对比矩阵/象限图（两维度比较）
  - 数据可视化（有具体数值）
  - 分层架构图（视觉层次感强）
  - 已有 Mermaid 的位置 → 在 Mermaid 下方加可视化版本

❌ 不适合：
  - 纯文字段落
  - 代码块
  - 表格已清晰的内容
  - 来源列表

输出：适合 qwen-image 的位置列表（位置描述 + 图类型 + 提示词草稿）

降级条件：
- 绘图指南文件不存在 → 跳过 Step 2 和 Step 3，直接进 Step 4 保存文字+Mermaid 版本
- 识别到 0 个适合位置 → 跳过 Step 3，直接进 Step 4
```

---

## Step 3：调用 qwen-image 生成图片并嵌入（内存操作）

**若 Step 2 识别到 0 个适合位置，跳过本步。**

> ❌ **严禁使用 Cursor 内置的 `GenerateImage` 工具**（那是给 UI 设计/图标生成用的，不是知识文档配图）
> ❌ **严禁只输出提示词让用户手动执行**——AI 必须自己调用 API
> ❌ **严禁引用 `article-image-angles` Skill**——那是公众号文章专用，不是知识库文档
> ✅ 唯一正确路径：加载 `ai-image-generator` Skill，按其绘图流程（Step 0~5）执行

**调用方式**：

```
Step 1：Read `.cursor/skills/ai-image-generator/SKILL.md`
Step 2：按 ai-image-generator 的绘图流程执行：
   - Step 0（对象-关系分析）
   - Step 1（分析段落逻辑）
   - Step 2（多视角草图，每张图至少2个视角）
   - Step 3（API 调用，优先 qwen-image-2.0-pro）
   - Step 4（更新配图索引，若无索引则创建）
   
模型选择：
   - 含中文文字的信息图/结构图 → qwen-image-2.0-pro（首选）
   - 纯视觉风格图 → wan2.6-t2i（次选）
   API 配置统一引用：_内部总控/凭证/AI能力配置.md
```

**嵌入格式（Mermaid 保留，qwen 图插入下方）**：

```markdown
```mermaid
[原 Mermaid 代码，保留不删]
```

> 📊 可视化版本：
![图描述](data:image/png;base64,[base64字符串])
```

**新位置（无 Mermaid）直接插入**：

```markdown
> 📊 [图标题]
![图描述](data:image/png;base64,[base64字符串])
```

**失败处理**：
- 单张图失败 → 跳过该图，继续其他图
- 全部失败 → 使用文字+Mermaid 版本，末尾标注「[图片生成失败，已保存文字版本]」

---

## Step 4：一次性写入文件 + REF-EXT 头部

> ⚠️ **写入前强制确认（不可跳过）**：
> 在执行写入之前，AI 必须自检：
> - Step 2 识别出了几个适合 qwen-image 的位置？
> - Step 3 实际生成/尝试了几张图？
> - 若两个数字均为 0，且用户未明确说「不要图片」或「跳过生图」：
>   → **不允许进入 Step 4，必须回到 Step 2 重新识别，或向用户确认是否真的跳过**
> - 允许继续的条件：① 生成了图片，OR ② 绘图指南不存在（已降级），OR ③ 用户明确说不要图

> ⚠️ 这是唯一的文件写入步骤。Steps 1-3 均在内存中操作，此步骤统一写入。
> ℹ️ **write-guard 例外**：本步骤写入 `L1_系统性文档/[维度]/知识库/` 路径，属于 REF-EXT 操作，`cognitive-structure-write-guard` 对此路径明确豁免（见 write-guard 触发条件例外条款）。无需版本快照/归因标注/L0更新。

**文件命名**：`[主题关键词]_调研_YYYYMMDD.md`

**目录检查**（写入前）：
```
检查 `_内部总控/认知结构/L1_系统性文档/[维度]/知识库/` 是否存在。
不存在则先创建：New-Item -ItemType Directory -Path [路径] -Force
```

**重复文件检查**（写入前）：
```
检查目标目录下是否有文件名含相同主题关键词的文件（完全同名才算）。
若有：询问「已有 [文件名]，更新现有文件还是新建？」等用户选择后继续。
```

**写入文件**（将 Step 1-3 的内存内容替换占位头部后一次写入）：

完整 REF-EXT 头部（替换 Step 1 的占位行）：

```markdown
> 文档类型：**REF-EXT**（外部参考知识）
> 调研日期：YYYY-MM-DD
> 调研目的：[从用户请求提取，一句话]
> 来源说明：AI网络调研产出（具体来源见文档末「参考来源」章节）
> 注意：本文档为外部知识汇编，不是郑总个人思考产物，不进入认知结构 L1/L2/L3
> 分类登记：`_内部总控/认知结构/文档分类清单.md` 区域十二
```

**保存失败时**：在对话中输出完整文档内容，告知用户建议手动保存到 `[正确路径]`。

⚠️ **写入成功后，不输出「完成」信号，立即继续 Step 5**。

---

## Step 5：自动注册三处（不可跳过）

**先执行：确定知识图谱 ID 序号**
```
Read: _内部总控/认知结构/知识图谱_正式文档.md
找 REF-EXT 区段中，以「[维度缩写]-」开头的最大序号。
新 ID = 最大序号 + 1（如已有 TECH-001、TECH-002 → 新建 TECH-003）。
若该维度无已有条目 → 从 001 开始。
维度缩写规则：技术架构=TECH / 认知科学=COGS / 产品理论=PROD / 待建维度=METH / 研究范式=RSCH / 组织理论=ORG
```

**①** `_内部总控/认知结构/文档分类清单.md` 区域十二末尾追加一行：
```
| _内部总控/认知结构/L1_系统性文档/[维度]/知识库/[文件名] | REF-EXT | [维度] | ★ CURRENT | [主题一句话描述，含调研日期] |
```

**②** `_内部总控/认知结构/知识图谱_正式文档.md` REF-EXT 区段末尾追加一行：
```
| [文档ID] | [主题简称] | [维度] | [核心内容一句话] | [维度]/知识库/ | [关联L1文档，不确定填「—」] |
```

**③** `_内部总控/认知结构/L3_原始记录/系统日志.md` 末尾追加一条：
```
[LOG-YYYYMMDD-NN] research-output | 调研：[主题] → [维度]/知识库/[文件名]，知识图谱ID：[ID]
```
> 注：此为认知结构变更日志，不同于 `_内部总控/任务日志.md`（任务执行日志）。

**注册完成后，告知用户**：
```
✅ 调研完成
📄 文档：[完整文件路径]
📌 知识图谱 ID：[ID]
📋 已注册到文档分类清单（区域十二）
📝 系统日志已更新
```

**任一注册失败时**：告知哪步失败，建议手动补注册；已成功的注册保留。

---

## 边界情况

| 情况 | 处理方式 |
|---|---|
| 主题跨维度 | 选更具体维度，注册时告知用户 |
| 用户说「不用存文件/只是问问」| 跳过 Step 4/5，只输出调研内容 |
| 目录不存在 | Step 4 写入前自动创建 |
| 同名文件已存在 | 询问「更新还是新建」 |
| 组合触发（调研+写文章）| 先完成本 Skill，再提示触发写作 Skill |

---

## 变更记录

### v1.0 — 2026-03-19 — 初始创建

**根因**：网络调研结果无持久化规范；AI 随机保存到错误位置，无图无注册。

**内容**：维度自动判断→V1含Mermaid→自审选图→qwen-image嵌入（Mermaid保留）→一次性保存→自动注册三处

**关卡A修复（3条严重）**：
- 绘图指南路径：认知科学维度 → 待建维度（正确路径）
- Step 3 API 调用：明确用 Shell+Python 脚本，附模板
- Step 5③ 系统日志：补全完整路径 `L3_原始记录/系统日志.md`

**关卡A修复（5条中等）**：
- 所有路径补全根目录前缀
- Step 2/3 跳过逻辑统一到 Step 3 开头
- Step 5② ID序号推导方式明确
- 组合触发场景（调研+写文章）处理规则
- 搜索工具指定（WebSearch + WebFetch）

**关卡A修复（3条严重）**：绘图指南路径/Step 3 API调用方式/系统日志完整路径
**关卡A修复（5条中等）**：路径前缀/跳过逻辑/ID序号/组合触发/搜索工具
**关卡B修复（2条Critical）**：
- C-1：Step 4 加 write-guard 例外注释；write-guard.mdc 新增知识库/路径例外条款
- C-2：维护协议矛盾修复（「不更新知识图谱」改为「追加REF-EXT区段，不计L1节点数」）
**关卡B修复（H-3）**：Step 2 绘图指南读取改为先尝试固定路径，失败后 Glob 搜索，仍未找到才降级并明确告知用户
**关卡C修复（2项）**：
- 触发判断补充「研究一下+调试关键词 → 不触发」条款
- Step 3 API 模板改为「先读 wechat-article-writer Step 4，复制其代码替换 prompt」，消除空引用
**验证状态**：✅ 关卡A/B/C 通过（场景5已修复，API引用已明确化）


---

## 变更记录

### 2026-03-19 — v1.0 → v1.1 — 修复触发词缺失+ 加强参考资料强制要求

**根因**：
1. 触发词缺少「大量调研」「全景调研」「调研一下」等常用表达，导致用户说「请大量调研」时 AI 未识别
2. 参考来源节格式模糊，只有「[来源1：描述 + 链接]」的占位符，无强制要求，导致生成的 REF-EXT 文档无参考资料

**修改内容**：
- 修改：触发判断 IF 条件 → 增加「大量调研」「全景调研」「调研一下」「搜一下」「查一下（需要结构化知识产出时）」
- 修改： Step 1 的「## 参考来源」模板 → 强制格式（作者/机构 | 标题 | URL | 访问日期），并标注强制执行

**验证方法**：
- 正向：下次用户说「大量调研XXX」时， AI 应识别并加载 research-output Skill
- 错误验证：单次问答（「说一下XXX是什么」）不应触发
- 参考资料：下次生成的 REF-EXT 文档应包含完整的参考资料节


### v1.1 → v1.2 — 2026-03-19 — thin wrapper 化（document-pipeline 统一入口）

**根因**：创建了 document-pipeline 统一写作流水线。research-output 变为入口路由层。
**修改内容**：新增 thin wrapper 转发块。原有 Step 0-5 保留为历史参考。
**验证状态**：🔵 待验证

---

### v1.2 → v1.3 — 2026-03-20 — 双修复：pre-save 强制图片门 + mode=improve 入口

**根因**：b2945f77 对话（思考过程分析）真实事件：AI 在未加载 research-output Skill 的情况下手动存了文档，跳过了 Step 2-3（图片识别+生成），导致用户追问时 AI 反向询问"用真实图片还是 Mermaid"。两条根因：① Step 4 写入前没有强制图片确认门；② 「完善已有 REF-EXT 文档」没有对应触发路径。

**修改内容**：

1. **Step 4 pre-save 强制门**：在 Step 4 标题下新增强制确认块——允许继续的条件是：生成了图片 OR 绘图指南不存在（已降级）OR 用户明确说不要图；三者均不满足时禁止写入，必须回到 Step 2
2. **触发判断新增 mode=improve**：用户说「完善/给...加图/按规范完善」时，以 mode=improve 从 Step 2 开始，跳过 Step 0-1（已有文档）和 Step 5（已注册）

**验证方法**：
- 正向①：下次 research-output 草稿完成未生图，Step 4 前应出现确认检查而非直接写文件
- 正向②：用户说「给 question_answering_cognitive_process.md 按规范加图」→ AI 应从 Step 2 入场
- 负向：用户明确说「跳过图片」时，仍可正常写入

**备份路径**：`history/SKILL_v1.2_20260320.md`

**验证状态**：🔵 待验证

---

### v1.4 → v1.5 — 2026-03-20 — Step 3 统一引用 ai-image-generator Skill

**根因**：v1.3/v1.4 修复后，Step 3 内部仍矛盾：外层说「先读 wechat-article-writer Step 4」，模板代码说「调用 ai-image-generator」。根因：ai-image-generator v1.1 早已存在但未被 research-output 正确引用（Glob 搜索方式失误导致漏发现）。

**修改内容**：Step 3 全部重写——删除「先读 wechat-article-writer Step 4」矛盾旧指令；统一改为「加载 ai-image-generator SKILL.md，按 Step 0~4 执行」；新增 ❌禁止引用 article-image-angles（公众号专用）

**验证方法**：下次执行 Step 3，AI 应加载 ai-image-generator 而非引用 wechat-article-writer

**验证状态**：🔵 待验证

**根因**：b2945f77 对话（思考过程分析）二次错误：用户确认要"方案B（真实图片）"后，AI 使用了 Cursor 内置的 `GenerateImage` 工具而不是 Shell+Python+dashscope qwen API，被用户叫停。同时 AI 的反思方向也错了——把 `article-image-angles`（公众号文章用）混淆为知识库文档的配图规范。

**修改内容**：Step 3 头部新增三条明确禁止：
1. ❌ 禁止使用 Cursor 内置 `GenerateImage` 工具
2. ❌ 禁止只输出提示词让用户手动执行
3. ✅ 明确唯一正确路径：Shell + Python + dashscope API（以 wechat-article-writer Step 4 为准）
4. 新增「混淆提示」：区分 article-image-angles / GenerateImage / 本 Skill 正确路径三种不同场景

**验证方法**：下次执行 Step 3 时，AI 应读 wechat-article-writer Step 4，用 Shell 工具执行 Python 脚本，不出现 GenerateImage 工具调用

**备份路径**：`history/SKILL_v1.2_20260320.md`（同 v1.3 备份）

**验证状态**：🔵 待验证

---

### v1.5 → v1.6 — 2026-03-21 — Step 1 末尾加强制 Step 2 提醒

**根因**：2026-03-21 project-retrospective 发现：执行 research-output 时，AI 完成 V1 草稿后直接跳到 Step 4 写文件，完全跳过 Step 2-3（绘图指南检查+图片生成），违反 Step 4 pre-save 强制门。根因是 Step 1 与 Step 2 之间无视觉提醒，AI 在长文档完成后「以为任务完成了」。

**修改内容**：Step 1 末尾（`**来源缺失时**` 之后、`---` 分隔符之前）新增强制检查点提示块：「⚠️ 草稿完成检查点（强制）：下一步必须执行 Step 2（绘图指南检查），不允许直接跳到 Step 4。Step 2 是必经检查点，即使调研内容很长、文字已写完，也不可跳过。」

**验证方法**：下次执行 research-output 时，AI 在完成 V1 草稿后应停顿并声明「进入 Step 2」，而非直接写文件。

**备份路径**：`history/SKILL_v1.5_20260321.md`

**验证状态**：🔵 待验证
